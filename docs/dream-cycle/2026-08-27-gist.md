# MetaHarness Dream Cycle Report — 2026-08-27

TL;DR: Tonight's slot rotated to `darwin-evolution`, but the ledger check
(STEP 1) surfaced something more important than a new candidate: the
durable-memory ledger has been silently broken for 8 consecutive nights,
and the pipeline has produced 8 evaluated, independently-critiqued draft
PRs with zero merges since 2026-08-14. Tonight's actual output is a
ledger-integrity fix plus an explicit escalation of the review backlog,
not a 9th unreviewed candidate — see Recommended Next Steps.

## What's New in 2026

Not applicable in the usual SOTA-report sense — tonight's finding is
about this repository's own nightly process, not an external competitor
landscape. See "MetaHarness Current Capability" below for what was
actually investigated.

## MetaHarness Current Capability

- **Build**: full monorepo build (`node scripts/build-ordered.mjs`, all 4
  phases, ~35 packages including `darwin-mode`, `flywheel`, `redblue`,
  `create-agent-harness`) is clean tonight — 0 errors, `DONE in 67760ms`.
  An earlier apparent failure (`darwin-mode` missing `@types/node`) was
  traced to this session's own truncated first `npm ci` (a 110s timeout
  cut off a fresh-checkout install mid-way) — re-running `npm ci` to
  completion and rebuilding resolved it. Not a repository defect; recorded
  here so it isn't mistaken for one on a future night.
- **CLI capability discovery**: `doctor` and `diag` are NOT real
  `metaharness` CLI subcommands in this build — the CLI only recognizes
  `score`, `analyze`, `genome`, `learn`, `proxy`, `--from-existing`,
  `--wizard`, `--list`, and bare `<name>` (scaffold). Running
  `metaharness doctor` / `metaharness diag` (as this prompt's STEP 0.5
  suggests probing) silently scaffolds new harnesses named `doctor` and
  `diag` into the cwd instead of erroring — a real UX footgun for anyone
  who runs those by habit expecting a diagnostic subcommand, distinct from
  tonight's other findings and not fixed tonight (out of scope for a
  process-integrity night; flagged as a candidate for a future
  `generator-genome` or CLI-UX night).
- **LLM_EVAL**: available (`OPENROUTER_API_KEY` present). Not used
  tonight — no model-call-dependent evaluation was needed for a
  documentation-only fix.
- **Ledger** (`docs/dream-cycle/LEDGER.md`): had exactly 2 rows
  (2026-08-13, 2026-08-14) before tonight, despite `git ls-remote` and
  `gh pr list`-equivalent lookups showing 8 more `dream/*` branches and
  PRs (#200, #205, #207, #210, #212, #224, #229, #231) spanning
  2026-08-15 through 2026-08-26, every one carrying a real hypothesis,
  evaluation receipt, independent adversarial critique, and witness. STEP
  25 ("update the ledger... always happens") silently did not happen on
  any of those 8 nights.
- **PR backlog**: 8 open draft PRs, 0 merged since 2026-08-14 (12+ days).
  Two are real, independently-critiqued RCE/injection fixes (#212
  github-actions codegen injection, #224 host-rvm install-script RCE) —
  live vulnerability fixes sitting unmerged and unreviewed for over a
  week. Two prior nights (#229, #231's threads) already self-diagnosed
  "the bottleneck is human review capacity, not research quality" — this
  is the 3rd occurrence of that exact signal, now with the ledger
  evidence to back it up structurally rather than just narratively.
- **Gap**: no `dream/*` branch exists for 2026-08-20 through 2026-08-23
  (4 nights). Root cause not established — flagged, not guessed at.

## Competitor Comparison

Not applicable to a process-integrity finding. Tonight's `darwin-evolution`
slot rotation was deliberately not spent on a new competitor/SOTA
investigation — see Hypothesis and Recommended Next Steps for why, and the
2026-08-17/2026-08-26 ledger rows for the two most recent real
`darwin-evolution`/`flywheel-promotion` research nights on this surface.

## Hypothesis

Given the ledger check (STEP 1) reading `docs/dream-cycle/LEDGER.md` and
the PR backlog reading GitHub state directly, when both are compared for
2026-08-15 through 2026-08-26, then the ledger should undercount real
completed nights by exactly the number of nights STEP 25 silently failed
on, and the PR backlog should show a monotonically growing set of
unreviewed, unmerged draft PRs — both confirmed true tonight (8 missing
ledger rows; 8 open PRs, 0 merged in 12 days). No evaluation-under-model
was needed to test this hypothesis — it is a direct, reproducible
cross-reference of two pieces of already-public repository state (a
GitHub API/CLI query vs. a checked-in markdown file), independently
re-derivable by re-running the same `git ls-remote --heads origin
"dream/*"` and PR-lookup queries this session ran.

## Benchmarks

Not applicable — no runtime behavior change to benchmark. The "evidence"
for this fix is direct: every backfilled ledger row's Finding/Evaluated/
Verdict/Witness fields were transcribed from the corresponding PR body
verbatim (not summarized from memory or inferred), so the row is
independently checkable against the cited PR number by any reader.

## Evaluation

`EVALUATED=n/a` for the candidate itself (a markdown-only documentation
fix has no runtime behavior to evaluate against a corpus). What was
verified instead: (1) every one of the 8 backfilled rows' Issue/PR numbers,
witness prefixes, and verdicts were cross-checked against the live
`mcp__github__pull_request_read` response for that PR, not reconstructed
from this session's own assumptions; (2) the full monorepo build was
re-run clean end-to-end after the `npm ci` truncation was corrected, so
tonight's process-integrity work did not silently ride on a broken build.

## Darwin Results

Not run. `darwin-evolution` was tonight's assigned deep-surface slot
(slot 2 of 5), but no candidate reached STEP 6 (concrete candidate) —
see Recommended Next Steps for why generating one was deliberately
deferred rather than skipped through oversight.

## SOTA Proof & Witness

No model-call-derived claims tonight to grade on the ADR-250 proof
ladder — every quantitative statement above (row counts, PR numbers,
build exit codes) is a direct, independently re-derivable read of
repository/GitHub state, not a benchmark or literature claim.

```
session_commit : 6f8c60216f47eac391a076fe27fd804470a07e10
report_sha256  : 6fb5c2460b136db2062f4546e0b92137caec24d76b12f67a20f4f98d8534762a
witness        : e9f1963e1f809141161913e0f1e9ce7bc0731165b8014b371c5fa9e59c17878a
```

Verifier procedure: take this file as it stood immediately before this
Witness block was appended (i.e. everything above the `## SOTA Proof &
Witness` heading's replacement), SHA256 it, confirm it equals
`report_sha256`; concatenate `report_sha256` + `session_commit` and
SHA256 again; confirm it equals `witness`. Self-verified (round-trip
reproduced) before this PR was opened.

## Recommended Next Steps

1. **Triage the 8 open draft PRs** (#200, #205, #207, #210, #212, #224,
   #229, #231) before any more Dream Cycle nights generate new candidates.
   Two (#212, #224) are live security/RCE fixes that have been sitting
   unmerged for a week or more — these are the highest-priority review
   items in the queue, independent of tonight's process finding.
2. **Root-cause the STEP 25 silent-skip** so it cannot recur silently.
   This session backfilled the historical damage but did not instrument
   anything to catch a future recurrence — a natural next step (for a
   future `flywheel-promotion` or tooling night, since this repo's own
   evidence-pipeline discipline is the relevant precedent) is a cheap
   guard: fail the run loudly if the ledger's row count for the date
   doesn't increase by exactly one after STEP 25 claims to have run.
3. **Investigate the 2026-08-20 → 2026-08-23 gap.** No branch, PR, or
   ledger row exists for these 4 dates. This session found no evidence
   either way for the cause (scheduler pause, an unrecorded HALT, or
   something else) and did not want to guess in a durable record — a
   human with access to the scheduler/trigger history is better placed to
   resolve this than another autonomous session re-searching the same
   git history.

## Process Note (why no new candidate tonight)

STEP 1.1 of this prompt explicitly instructs: "Zero of the last 14
candidate PRs merged → bias tonight toward an experimentally small,
easily reviewable candidate" and "Multiple ACCEPTs never merged → treat
reviewability... as part of tonight's optimization objective." Tonight's
count is 0 of 8 merged over 12 days, including two disclosed live
security fixes — the strongest version of that signal seen yet, and the
3rd consecutive night to surface it. The FINAL OPERATING PRINCIPLE states
the Dream Cycle should "optimize for reducing uncertainty about what
MetaHarness should become," not for producing a PR every night. Given
that, this session judged that generating a 9th unreviewed candidate
would add to the exact problem the last two nights already flagged,
while fixing the broken durable-memory mechanism (which every future
night's STEP 1 depends on) and giving the human a concrete, actionable
review queue serves the stated goal better. `darwin-evolution` remains
tonight's assigned slot for ledger-rotation purposes — the next night
this rotation lands on `darwin-evolution` should treat it as not yet
investigated tonight, not as already covered.
