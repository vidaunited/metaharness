# Dream Cycle 2026-09-01 — Flywheel Evidence Record

Durable evidence for tonight's flywheel-promotion candidate, classified per the Dream Cycle
knowledge-type discipline.

## OBSERVATION

Three consecutive flywheel-promotion Dream Cycle nights (2026-08-16 PR #205, 2026-08-26 PR #231,
2026-08-31 PR #255) each disclosed and fixed a real, distinct trust-boundary gap in
`verifyReplayBundle`/`gateReExecutes` (`packages/flywheel/src/replay.ts`). All three remain open,
unmerged draft PRs as of tonight — along with 10 other Dream Cycle PRs from the same 17-night window,
none merged. Confirmed by direct GitHub state inspection (`pull_request_read`), not the (sparse,
under-populated) `LEDGER.md` alone: `git ls-remote --heads origin "dream/*"` lists 13 branches from
2026-08-15 through 2026-08-31; `search_pull_requests` confirms 13 open, 0 merged in that window
(vs. 2/2 merged for the two nights before it, 2026-08-13/08-14).

An architecture-review subagent (no shared authorship context with the sessions that produced #205/
#231/#255) confirmed by direct experiment (git worktree, cherry-pick in dependency order) that the
three fixes are semantically compatible but git-conflicting: all three independently rewrite the same
~15-line region of `gateReExecutes`'s loop body, because each was authored against a base predating
the others.

## MEASUREMENT

- **Consolidation build**: hand-merged (not textually rebased) all three fixes into
  `packages/flywheel/src/{replay.ts,run.ts,cli.ts}` + `__tests__/units.test.ts` against current main
  (`b611993d7088dff877f5713e41031a714e77bfc0`). `npm run build -w @metaharness/flywheel`: clean.
  `npx vitest run packages/flywheel`: 71/71 (before the 4th-gap fix below).
- **Non-vacuous proof, 3-way consolidation**: stashing only the source files (keeping the merged test
  file) and running against unpatched (main) `replay.ts`/`run.ts` produced **18/41 failing test
  assertions** in `units.test.ts` — the exact new checks this candidate adds, nothing else. Restoring
  the fix returns to 71/71 (then 72/72, see below).
- **Independent adversarial critic** (fresh sub-agent, no authorship context) reviewed the consolidated
  diff and constructed a working exploit: `BOUND_KEYS` (the `sealedFieldsAuthentic` check from #255)
  bound `id`/`target`/`verdict`/`primaryDelta`/score fields/`failureReasons`, but not `parents` or
  `generation` — two genuinely, independently signed PROMOTED commits could be spliced into a
  fabricated multi-generation chain by rewriting only those two unsigned fields, defeating
  `contiguousParents`/`reachesRoot`'s guarantee entirely, with **every** check (including
  `sealedFieldsAuthentic`) reporting PASS.
- **4th-gap fix + non-vacuous proof**: added `parents`/`generation` to `BOUND_KEYS` and to the signed
  payload in `run.ts`, plus a new regression test reproducing the critic's exact exploit. Verified
  non-vacuous directly: temporarily removing the two new `BOUND_KEYS` entries reproduces the critic's
  exploit exactly (`expected true to be false`, i.e. `sealedFieldsAuthentic` wrongly reports PASS on the
  spliced chain); restoring them closes it. Final: `npx vitest run packages/flywheel` → **72/72**.
- **Real-bundle regression check** (not just synthetic fixtures) — every committed `ReplayBundle`/
  proof-bundle file in the repo re-verified against the patched, built `dist`:
  `packages/radio/.radio-flywheel/replay-bundle.json`,
  `kimi-k3-harness/.harness/flywheel/replay-bundle.json`,
  `packages/darwin-mode/bench/swebench/proof-bundle-swebench.json`,
  `experiments/signal-flywheel/bundle.json` (also via its own `verify.mjs`: PASS, all 8 checks green),
  `packages/evals-math/bench/proof-bundle-gsm8k*.json` (3 files) — **all 7 `pass: true`, unchanged**.
- **Broader regression check**: full monorepo `npm run build` (4 phases, clean) and
  `npx vitest run packages/flywheel packages/evals-math packages/darwin-mode` → 77 test files,
  **715/715 non-skipped tests green**, 0 failures.

## INFERENCE

The consolidated diff genuinely closes all four disclosed/found gaps without weakening the trust model
elsewhere: every new check is additive and opt-in-by-payload-key, so no honestly-produced bundle
(synthetic or real) is affected, while a forged bundle exploiting any of the four gaps now fails
replay. This is verified, not assumed — by direct before/after non-vacuous reproduction for every gap,
including the one this session's own critic found rather than inherited from a prior night.

## DECISION

Ship as ADR-274 (renumbered twice on 2026-09-01: ADR-272 → ADR-273 after #260 independently claimed
ADR-272 first, then ADR-273 → ADR-274 after PR #179's merge independently claimed ADR-273 for the
unrelated "Dream Machine" ADR — see the merge-conflict resolution note in tonight's ledger row),
superseding #205/#231/#255
in effect (their logic is fully subsumed here, plus one
closed gap they did not find). Recommend to the human reviewer: close #205/#231/#255 once this PR
merges, since re-reviewing three additional overlapping diffs after this one lands would be pure
review-burden with no additional evidence value.

## REJECTION

None — no candidate direction was rejected tonight; the alternative considered (a 4th independent
stacked PR closing only the parents/generation gap) was rejected at the STEP 3.2 candidate-scoring
stage in favor of consolidation, not evaluated and found wanting (see tonight's gist, Candidate
Selection).
