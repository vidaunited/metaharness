# agent-harness-generator — ADR Index

> Read top-to-bottom. The series is structured so that earlier ADRs ground the later ones; if you jump in the middle you will hit forward references that have not yet been justified.

## How to read this set

Each ADR is independently reviewable. The early series (ADR-001…019) is design documentation the repo is built against; the later, `Accepted` ADRs (e.g. ADR-019 release orchestration, ADR-020/021 the web UI) document decisions whose code has already landed. The naming convention is `ADR-NNN-kebab-case-slug.md` with three-digit padding.

Every ADR follows the same shape:

1. **Title / Status / Date / Related**
2. **Context** — what problem we are solving and why now
3. **Decision** — what we are doing about it
4. **Consequences** — what changes, what does not, what hurts
5. **Alternatives Considered** — what we rejected and why
6. **Test Contract** — what tests must exist for this decision to be considered shipped (London-school for kernel units; integration for generator output; contract tests for host adapters)
7. **References** — citations to prior art, papers, ruflo ADRs

## The phases

| Phase | What ships | ADRs that define it |
|---|---|---|
| **0 — Kernel extraction** | `@metaharness/kernel` package (Rust → wasm + NAPI-RS); refactor strategy from ruflo | ADR-001, ADR-002, ADR-002a |
| **1 — Generator MVP** | `npx create-agent-harness <name>` works for Claude Code | ADR-003, ADR-007, ADR-010, ADR-011 |
| **2 — Multi-host** | Codex, pi.dev, Hermes adapters | ADR-004 |
| **3 — Composer** | Interactive agent / skill / plugin picker; `--from-existing` eject mode | ADR-003 §3, ADR-012 |
| **4 — Marketplace publication** | Generator is itself a marketplace plugin; generated harnesses can publish | ADR-005, ADR-009 |
| **5 — Self-evolving + federation** | Learning loop applied to harness-level optimisation; multi-instance federation | ADR-014 |
| **6 — Vertical packs** | Curated bundles (`@metaharness/vertical-legal`, `@metaharness/vertical-trading`, …) | ADR-013 |
| **7 — Darwin Mode** | Population-based, bounded self-improvement of the harness: generate child variants, sandbox-score them, archive lineage, promote measured wins; rigorous parent-vs-child benchmarking; the SOTA "Darwin Plus" stack (DGM + HGM + SGM + Hyperagent-lite) | ADR-070…081 |

ADR-006 (memory + learning) and ADR-008 (drift detection) cut across all phases. ADR-015 (naming) and ADR-016 (migration) are phase-independent.

## The ADRs

| # | Title | Status | One-line summary |
|---|---|---|---|
| [ADR-001](./ADR-001-goals-and-non-goals.md) | Goals and non-goals | Proposed | What the project is, is not, and what success looks like. |
| [ADR-002](./ADR-002-kernel-boundary.md) | Kernel boundary | Proposed | What goes in `@metaharness/kernel`, what is content. The load-bearing decision of the series. Kernel is Rust → wasm + NAPI-RS. |
| [ADR-002a](./ADR-002a-rust-wasm-napi-publishing-pipeline.md) | Rust crate + WASM/NAPI-RS publishing pipeline | Proposed | Cargo workspace layout, multi-target CI matrix, lockstep version contract. Read after ADR-002. |
| [ADR-003](./ADR-003-generator-architecture.md) | Generator architecture | Proposed | How `create-agent-harness` works: templates, renaming, composition. |
| [ADR-004](./ADR-004-host-integration-model.md) | Host integration model | Proposed | Adapter abstraction across Claude Code, Codex, pi.dev, Hermes. |
| [ADR-005](./ADR-005-marketplace-plugin-design.md) | Marketplace plugin design | Proposed | How the generator and its outputs participate in the IPFS plugin registry. |
| [ADR-006](./ADR-006-memory-and-learning-integration.md) | Memory + learning integration | Proposed | How generated harnesses inherit AgentDB + HNSW + ReasoningBank + emergent-time decay. |
| [ADR-007](./ADR-007-ci-guards.md) | CI guards | Proposed | What GitHub Actions gates must pass before generator/harnesses can publish. |
| [ADR-008](./ADR-008-drift-detection.md) | Drift detection | Proposed | Detecting and recovering kernel/template/generated-harness drift. |
| [ADR-009](./ADR-009-anti-slop.md) | Anti-slop | Proposed | Marketplace quality model: smoke tests, signals, reputation, gates. |
| [ADR-010](./ADR-010-tdd-test-contracts.md) | TDD test contracts | Proposed | Test strategy per phase: London-school unit, integration, contract. |
| [ADR-011](./ADR-011-witness-and-provenance.md) | Witness + provenance | Proposed | Ed25519 manifests for generated harnesses (mirrors ruflo ADR-103). |
| [ADR-012](./ADR-012-eject-and-upgrade-strategy.md) | Eject + upgrade strategy | Proposed | How generated harnesses receive kernel updates. Vendored vs peer-dep. Eject mode. |
| [ADR-013](./ADR-013-vertical-packs-publishing.md) | Vertical packs publishing | Proposed | How curated bundles get published, owned, maintained. |
| [ADR-014](./ADR-014-self-evolution-and-federation.md) | Self-evolution + federation (exotic) | Proposed | Learning loop applied at harness level; federated multi-instance harnesses. |
| [ADR-015](./ADR-015-naming-and-branding-policy.md) | Naming + branding policy | Proposed | Independence mode vs powered-by; scope strategy; marketplace tags. |
| [ADR-016](./ADR-016-migration-for-ruflo-users.md) | Migration for existing ruflo users | Proposed | Moving from ruflo to a generated harness without losing memory / patterns / skills. |
| [ADR-018](./ADR-018-rvm-as-deployment-target.md) | RVM as deployment target | Accepted | Use the RVM microhypervisor as the hardware-isolation tier for federation + multi-tenant deployments. |
| [ADR-019](./ADR-019-release-orchestration.md) | Release orchestration | Accepted | `scripts/release.mjs` composes version-bump + preflight + marketplace + dry-run + tag into one command; refuses dirty tree; per-step PASS/SKIP/FAIL gating. |
| [ADR-020](./ADR-020-web-generator-ui.md) | Web generator UI | Accepted | `apps/web-ui` — client-only React/Vite generator. Composes harnesses + Claude skills/agents/commands, live preview, zip download. Renderer ported behaviour-for-behaviour from the CLI; parity test pins it. |
| [ADR-021](./ADR-021-client-side-packaging-and-pages-deploy.md) | Client-side packaging + Pages deploy | Accepted | JSZip + Blob in-browser packaging (deterministic dates); `VITE_BASE` env-driven base path; gated GitHub Pages workflow (unit + e2e before deploy); UI isolated from the kernel workspace. |
| [ADR-022](./ADR-022-mcp-primitive.md) | MCP as a modular, gated, security-first primitive | Accepted | MCP is one selectable primitive (off/local/remote), default-deny. Emits gated `src/mcp/*` + scannable `mcp-policy.json`; `harness mcp-scan` is "npm audit for agent tools"; policy is witness-bound. |
| [ADR-023](./ADR-023-repo-to-harness-importer.md) | Repo → Harness importer | Accepted | Paste a GitHub URL → deterministic file inventory + archetype scoring → editable harness plan. Embeddings recommend, rules generate, tests prove parity; no repo code executed. |
| [ADR-024](./ADR-024-studio-and-verify.md) | Agent Harness Studio + in-browser Verify | Accepted | Four-tab Studio (Repo→Harness / Create harness / Skill·Agent·Command / Verify). `verifyFileMap` validates a dropped zip in-browser — structure + MCP policy + secrets — no upload. |
| [ADR-025](./ADR-025-browser-embeddings-minilm.md) | Browser embeddings (Transformers.js MiniLM) | Accepted | Implements the ADR-023 `semantic` term with optional, lazy, WebGPU/WASM MiniLM (`all-MiniLM-L6-v2`). Injected into the rule-based scorer; rounded for determinism; lexical fallback stays the default so the deploy needs no model. |
| [ADR-026](./ADR-026-cli-repo-analyzer-ruvllm.md) | CLI repo analyzer + ruvllm | Accepted | `harness analyze-repo [path]` — local, analysis-only repo → plan, mirroring the browser core. Opt-in `--embed` via `@ruvector/ruvllm` (deterministic, offline, optionalDependency) with lexical fallback; writes repo-profile.json + harness-plan.json. |
| [ADR-027](./ADR-027-cli-and-web-ui-integration.md) | CLI and Web-UI integration | Accepted | Byte-parity contract between `packages/create-agent-harness/` (CLI) and `apps/web-ui/` (Studio); decoupled release cadence (npm tag-driven vs Pages push-driven); asymmetric-features table; 6 required tests; manifest.meta.surface + meta.kernel_version diagnostics (iter 56). |
| [ADR-028](./ADR-028-skew-detection-and-liveness.md) | Skew Detection and Liveness | Accepted | Two orthogonal axes (skew: informational, liveness: blocking). Single HTTP probe implementation in `healthcheck` delegated to by `release.mjs` preflight (iter 77) and `pages.yml` verify (iter 78). `skewVerdict()` is the one comparator; `manifest.meta.*` + `resolveLocal<X>Version()` is the recipe for adding a new skew axis. JSON-from-text delegation pattern keeps formatter pairs in lockstep. |
| [ADR-029](./ADR-029-cross-language-invariants.md) | Cross-Language Invariants and Defense-in-Depth Catalog Gates | Accepted | 4-layer defense-in-depth catalog gate ordered by failure-surface latency: `healthcheck.catalogCount` (iter 86) → CI `healthcheck` step → `vertical-tour` per-push gate (iter 89) → per-template structural tests. Pattern for any "same count in N languages" invariant: JSON wins ties, TS + Rust extract via regex, healthcheck pins them together. iter-83 drift class shifts from CI-N+2 to pre-push. |
| [ADR-030](./ADR-030-discovery-loop.md) | The Discovery Loop | Accepted | 5-step propagation for every user-facing tool: build → surface in README/skill → catalog in dev-toolkit/plugin.json → discover contextually (Next: block on FAIL only) → test the propagation. iter-90 bundle reached step 4 across iter 90→94. "Next:" block contract: command using user's actual path + URL + reassurance about cost (sanitisation, etc.). Tests pin FAIL has suggestion AND HEALTHY doesn't. |
| [ADR-031](./ADR-031-bundle-json-pattern.md) | The Bundle JSON Pattern | Accepted | Schema-1 envelope for `--bundle` JSON snapshots, evidenced by 3 implementations (iter 90 diag, iter 97 export-config, iter 102 audit). 6 rules: schema/generatedAt/exitCode envelope, mandatory sanitisation with iter-97 canonical regex, errors-are-bundle-formed, exit code follows verdict, text mode unchanged when `--bundle` added, no shared helper at N=3 (revisit at 4). |
| [ADR-032](./ADR-032-host-copilot.md) | GitHub Copilot as a harness host | Implemented (iter 127) | `@metaharness/host-copilot` — the 7th host adapter. Emits `.vscode/mcp.json` (both `servers` + `mcpServers` keys for VSCode 1.99+ AND Claude Code compat). MCP policy layer unchanged. |
| [ADR-033](./ADR-033-host-github-actions.md) | GitHub Actions as a harness host | Implemented (iter 146–147) | `@metaharness/host-github-actions` — headless CI/CD host, the first NON-INTERACTIVE one. Composite action in `.github/actions/<name>/`, workflow YAML in `.github/workflows/<name>.yml`. Default-deny via the workflow `permissions:` block (`permissionsBlock()` maps allow-list → least-privilege GITHUB_TOKEN scopes). 9th host; propagated into HOSTS catalog + bench + web-ui + CI; `metaharness@0.1.5`. |
| [ADR-034](./ADR-034-oia-integration.md) | Open Intelligence Architecture (OIA) Integration | Proposed | OIA (Agentics Foundation, v0.1) is a nine-layer reference architecture for enterprise intelligent systems — not a runtime. Plugs in as a **cross-cutting manifest layer**: every harness optionally emits `.harness/oia-manifest.json` declaring layer alignment and adjacent-standard wiring. Toggle: `oia: 'off' \| 'manifest-only'`. Default-deny MCP posture takes precedence over any future OIA identity claim. Implementation deferred; 4 open questions gating a follow-on ADR (layer names, registry endpoint, identity composition, v1.0 migration). |
| [ADR-035](./ADR-035-product-naming.md) | Product naming — MetaHarness | Accepted | Documents the iter 108→124 naming journey (agentmint → mintagent → openharness → metaharness) and locks down: **brand = MetaHarness**, **npm = `metaharness`** (one-word, unscoped, published iter 124), **library = `@ruvnet/agent-harness-generator`**, **repo = `ruvnet/agent-harness-generator`** (category, unchanged). Rule from iter 117: *before generation = `metaharness`; inside generated harness = `harness`*. Defensive moat: npm's similarity rule blocks `meta-harness` (dashed) from being squatted by anyone else. |
| [ADR-036](./ADR-036-host-opencode.md) | OpenCode as a harness host (8th host) | Implemented (iter 128) | `@metaharness/host-opencode` — sst/opencode is an open-source terminal AI coding agent, MCP-native by design. The **8th host adapter** following the ADR-032 pattern: config-emitter writing `.opencode/opencode.json` + `install.md`. Schema-compatible with ADR-022 MCP — no protocol bridge needed. Default-deny composition: copies `mcp-policy.json` deny rules verbatim into OpenCode's `mcp.permissions.deny` so the harness's posture wins via OpenCode's own enforcement. |
| [ADR-046](./ADR-046-real-install-host-verification.md) | Real-install host verification (caught 3 schema bugs) | Implemented (2026-06-16) | Installed + ran each feasible host runtime against generated harnesses. claude-code (`claude -p`), codex (`codex exec` via OpenRouter), opencode (`opencode run`), github-actions (`act` in Docker) all run; openclaw `config validate` passes. Caught + fixed 3 real schema bugs that schema-shape + live-content checks missed: opencode (`mcp` is a direct map + top-level `permission`, not `mcp.servers`/`mcp.permissions`), openclaw (`mcp.servers.<name>` with `enabled`, no top-level permissions), hermes (`model:`+`agent.personalities`, no name/system_prompt/scrub keys). pi-dev (no clean install), rvm (AArch64 bare-metal), copilot (interactive VSCode) documented as not-locally-runnable. |
| [ADR-045](./ADR-045-cli-scaffold-host-adapter-wiring.md) | CLI scaffold host-config wiring for all 9 hosts | Implemented (2026-06-16) | Found during the ADR-044 live verification: `npx metaharness <name> --host <non-claude>` records the host in the manifest but emits only `.claude/*` config — the CLI scaffolder renders claude-shaped `templates/` and never calls the (now spec-complete) `@metaharness/host-*` adapters. Three host-config codegen paths exist; only the adapters + web-UI cover all 9 hosts. Proposed fix: build a HarnessSpec post-`walkTemplate`, call each host's `generateConfig`, merge; + make `verify-all-hosts.mjs` scaffold via the real `--host` path so it can't silently regress. Deferred (cross-cutting wiring change). |
| [ADR-044](./ADR-044-live-llm-host-verification-and-capability-coverage.md) | Live-LLM host verification & host capability coverage review | Proposed | Closes two gaps found reviewing all 9 `host-*` adapters: (1) `verify-all-hosts.mjs` is schema-only for 8/9 hosts (real `claude -p` only for claude-code) — adds `scripts/verify-harness-live.mjs`, a provider-agnostic live verifier via OpenRouter (`OPENROUTER_API_KEY` GCP secret, ~$0.0002/host, SKIPPED without key); (2) adapters under-consume `HarnessSpec`. Capability matrix + 3 bug-grade findings (opencode permissions read wrong field; opencode drops agents; rvm capability table built from empty array). Drives coverage to 100% via `REVIEW-PROGRESS.md`, CLI hosts first then web-UI. opencode fixed first (iter 1). |
| [ADR-037](./ADR-037-draco-deep-research-benchmark.md) | DRACO — cross-domain deep-research benchmark with OpenRouter fusion | Proposed | The missing quality gate for `vertical:research`. Versioned cross-domain corpus (≥50 Q, machine-checkable rubrics) + OpenRouter fusion harness (each pipeline stage routes to a different model class; a DIFFERENT strong model verifies than synthesises — fusion's point). 5-dimension scoring (grounding via citation re-fetch, coverage, balance, LLM-judge faithfulness, efficiency). Proof = committed re-runnable `runs/<iso>.json` + per-domain breakdown; `bench-baseline` regression gate. `OPENROUTER_API_KEY` via the iter-145 GCP secret-list gate. 6 horizon-tracked milestones (M1 corpus → M6 fusion-vs-single ablation). Owned by `ruflo-goals:horizon-tracker`. |
| [ADR-047](./ADR-047-algorithmic-agent-harness.md) | The Algorithmic Agent Harness — algorithms as the control plane | Proposed | `@metaharness/harness` — a dependency-free, deterministic control plane that wraps any model/tool/skill as a replaceable **worker**. Composes the series' pieces (router ADR-040/043, receipts ADR-011, MCP gating ADR-022, learning ADR-014) into the 10-step execution loop. Key invariant: `decision = argmax utility(action)`; no action runs unless `confidence ≥ threshold ∧ risk ≤ budget ∧ cost ≤ budget ∧ verification == pass`. One algorithm per module (softmax intent router · UCB1 bandit pool · adversarial verifier + critique loop · default-deny policy gate · circuit breaker + retry budget · weighted-majority/Borda consensus · hash-chained tamper-evident receipts). 44 unit tests; "the model proposes, the harness decides, the algorithms verify." |
| [ADR-038](./ADR-038-draco-beyond-sota-ruflo-components.md) | DRACO beyond-SOTA — optimizing the research harness with ruflo components | Proposed | Layering ruflo intelligence components onto the DRACO research harness to push past the SOTA baseline on quality. |
| [ADR-039](./ADR-039-draco-cost-efficiency-beyond-sota.md) | DRACO beyond-SOTA on the COST axis — cheap-model dominance | Proposed | The cost-axis companion to ADR-038: cheap-model routing dominating expensive single-model baselines on $/quality. |
| [ADR-040](./ADR-040-adaptive-cost-optimal-routing.md) | Adaptive Cost-Optimal Routing — benchmark to operational router | Proposed | Turns the DRACO routing matrix + oracle into an operational router (`router_v2`) with a measured oracle gap. |
| [ADR-041](./ADR-041-metaharness-as-program-synthesis.md) | MetaHarness as program synthesis + search | Proposed | Reframes harness generation as program synthesis under search: search designs, simulate, score, mutate topology, emit best artifact + scorecard. The scorecard is the differentiator. |
| [ADR-042](./ADR-042-beta-feedback-credibility-reconciliation.md) | Beta-feedback triage — credibility reconciliation (issue #4 / AQE fleet) | Proposed | Reconciling conflicting beta feedback by credibility weighting; triage method for the AQE fleet. |
| [ADR-043](./ADR-043-router-training-pipeline.md) | Router training pipeline — kernel ridge regression over the routing dataset | Proposed | The training pipeline behind ADR-040: kernel ridge regression over the routing dataset to learn the router. |
| [ADR-048](./ADR-048-formal-proof-swarm-ruvnet-algo-stack.md) | A Formal-Proof Swarm for `unsorry` — orchestrating the ruvnet algo stack | Proposed | Orchestrating the ruvnet algorithm stack as a swarm to discharge Lean-kernel-verified proofs in `unsorry`. |
| [ADR-049](./ADR-049-nontrivial-hard-theorem-strategy.md) | Sourcing & proving non-trivial (harder) theorems for `unsorry` | Proposed | Strategy for sourcing and proving harder, non-trivial theorems — value × tractability admission for the proof swarm. |
| [ADR-050](./ADR-050-harness-intelligence-from-verified-swarm.md) | Harness intelligence learned from the verified-swarm push (cross-host) | Proposed | Promotes five transferable patterns from the verified-swarm push (verification-gated output, decompose-on-failure, compounding memory, value×tractability admission, …) to host-agnostic capabilities. |
| [ADR-051](./ADR-051-third-party-sdk-showcase-examples.md) | Third-party SDK showcase examples (`npx @metaharness/example-*`) | Proposed | The umbrella ADR for the `@metaharness/example-*` SDK showcases: structure, naming, and the gate each example must pass. |
| [ADR-052](./ADR-052-example-aws.md) | example-aws — Amazon Web Services SDK showcase | Proposed | A generated harness showcasing the AWS SDK as a worked example. |
| [ADR-053](./ADR-053-example-gcp.md) | example-gcp — Google Cloud Platform SDK showcase | Proposed | A generated harness showcasing the GCP SDK. |
| [ADR-054](./ADR-054-example-azure.md) | example-azure — Microsoft Azure SDK showcase | Proposed | A generated harness showcasing the Azure SDK. |
| [ADR-055](./ADR-055-example-stripe.md) | example-stripe — Stripe SDK showcase | Proposed | A generated harness showcasing the Stripe SDK. |
| [ADR-056](./ADR-056-example-slack.md) | example-slack — Slack SDK showcase | Proposed | A generated harness showcasing the Slack SDK. |
| [ADR-057](./ADR-057-example-github.md) | example-github — GitHub SDK showcase | Proposed | A generated harness showcasing the GitHub SDK. |
| [ADR-058](./ADR-058-example-twilio.md) | example-twilio — Twilio SDK showcase | Proposed | A generated harness showcasing the Twilio SDK. |
| [ADR-059](./ADR-059-example-datadog.md) | example-datadog — Datadog SDK showcase | Proposed | A generated harness showcasing the Datadog SDK. |
| [ADR-060](./ADR-060-example-supabase.md) | example-supabase — Supabase SDK showcase | Proposed | A generated harness showcasing the Supabase SDK. |
| [ADR-061](./ADR-061-example-huggingface.md) | example-huggingface — Hugging Face SDK showcase | Proposed | A generated harness showcasing the Hugging Face SDK. |
| [ADR-062](./ADR-062-example-pinecone.md) | example-pinecone — Pinecone SDK showcase | Proposed | A generated harness showcasing the Pinecone SDK. |
| [ADR-063](./ADR-063-example-fhir.md) | example-fhir — Health / Medical Devices (FHIR) SDK showcase | Proposed | A generated harness showcasing a FHIR (health/medical) SDK. |
| [ADR-064](./ADR-064-example-ads.md) | example-ads — Ad Platforms SDK showcase | Proposed | A generated harness showcasing ad-platform SDKs. |
| [ADR-065](./ADR-065-example-web3.md) | example-web3 — Blockchain / web3 SDK showcase | Proposed | A generated harness showcasing a blockchain / web3 SDK. |
| [ADR-066](./ADR-066-example-iot.md) | example-iot — IoT / Robotics SDK showcase | Proposed | A generated harness showcasing an IoT / robotics SDK. |
| [ADR-067](./ADR-067-example-nasa.md) | example-nasa — Space / Satellites SDK showcase | Proposed | A generated harness showcasing a space / satellites (NASA) SDK. |
| [ADR-068](./ADR-068-example-qiskit.md) | example-qiskit — Quantum Computing SDK showcase | Proposed | A generated harness showcasing the Qiskit quantum-computing SDK. |
| [ADR-069](./ADR-069-example-bio.md) | example-bio — Bioinformatics SDK showcase | Proposed | A generated harness showcasing a bioinformatics SDK. |
| [ADR-070](./ADR-070-darwin-mode-self-improving-harness.md) | Darwin Mode — bounded empirical self-improvement | Proposed | Head of the Darwin Mode series. The harness (not the model) self-improves: generate child variants, sandbox-score them, archive lineage, promote measured wins. `npx metaharness evolve <repo>`. Level-2 (harness) self-improvement is the wedge. |
| [ADR-071](./ADR-071-darwin-mutation-surfaces-safety-allowlist.md) | Darwin Mode — mutation surfaces + safety allowlist | Proposed | Seven approved mutation files (planner/context/reviewer/retry/tool/memory/score); everything else blocked. A pre-execution `inspectVariant` gate + `validateGeneratedCode` validator enforce the bound. Ten containment rules. The gate is code, not a comment. |
| [ADR-072](./ADR-072-darwin-scoring-and-promotion.md) | Darwin Mode — scoring + promotion model | Proposed | Weighted score (0.35 task + 0.20 tests + 0.15 trace + 0.10 cost + 0.10 latency + 0.10 safety) minus a hard penalty layer; strict promotion gate (delta + safety≥0.95 + no test regression + no blocked writes). The child cannot edit the benchmark or the scorer. |
| [ADR-073](./ADR-073-darwin-archive-and-selection.md) | Darwin Mode — the archive (evolve like species) | Proposed | Keep a *tree* of variants, not a single best branch — a weak ancestor can seed a strong branch. Non-promoted variants are retained; selection samples from the whole archive (stall fallback + descendant-potential bias). Lineage is the demo and the provenance. |
| [ADR-074](./ADR-074-darwin-ruvector-memory-ruflo-fabric.md) | Darwin Mode — ruVector memory + RuFlo fabric | Proposed | ruVector is cross-repo evolutionary memory (`put`/`search` mutations, traces, score deltas → transferable learning); RuFlo orchestrates the population (cartographer · mutation designer · patch engineer · test runner · trace critic · safety auditor · archive curator · promotion judge). Both optional with fallbacks. |
| [ADR-075](./ADR-075-darwin-prototype-roadmap-and-acceptance.md) | Darwin Mode — prototype roadmap + acceptance | Proposed | Stage the build: Prototype 1 reviewer (safest) → Prototype 2 context builder (fastest gain) → Prototype 3 full harness. 30-day target: ≥15% task-success gain with traceability + rollback. Three-repo acceptance (ruVector, this repo, ruQu): 10 variants × 20 tasks, ≥10% gate, reproducible winner. |
| [ADR-076](./ADR-076-darwin-parent-vs-child-benchmark.md) | Darwin Mode — benchmark the parent vs the child | Proposed | The rigorous evaluation layer (`evolve --bench`): five gates (solve · regression · safety · cost · repro), a verified-solve score (0.40 verified_solve dominates) + penalties, and statistical promotion (child mean > parent + 0.05 **and** lower-95% seeded-bootstrap delta > 0, hidden tests up, zero safety violations, clean replay). Descendant-potential-aware parent selection (0.70 score + 0.20 potential + 0.10 diversity) picks the best **branch**, not the best agent. Anti-gaming controls (immutable task-hash snapshots, diff guard, metered cost, scorer outside the variant). |
| [ADR-077](./ADR-077-dgm-empirical-harness-evolution.md) | Darwin Gödel Machine — empirical harness evolution (foundation) | Proposed | Grounds Darwin Mode in the DGM paper (arXiv:2505.22954): frozen model, evolve the harness not the prompt, archive of diverse agents, sandboxed empirical validation. Maps each DGM element (SWE-bench 20→50, Polyglot 14.2→30.7; discovered gains = editing tools, long context, peer review) to a concrete module. |
| [ADR-078](./ADR-078-hgm-clade-metaproductivity-selection.md) | Huxley–Gödel Machine — clade metaproductivity selection | Proposed | Grounds clade selection in HGM (arXiv:2510.21614): the Metaproductivity–Performance Mismatch — best agent ≠ best parent. `lineage.ts` selects by descendant potential (CMP approx) + diversity (+ safety stability in clade mode), picking the best **branch**. Thompson-sampling expansion is the next increment. |
| [ADR-079](./ADR-079-sgm-statistical-gates-risk-budget.md) | Statistical Gödel Machine — statistical gates + global risk budget | Proposed | Grounds the safety layer in SGM (arXiv:2510.10232): admit a self-modification only when superiority is statistically certified (seeded bootstrap CI as the practical stand-in for e-values/Hoeffding) **and** a global cumulative risk budget can absorb it. `stats.ts`/`promotion.ts`/`risk.ts`; `admitWithStatisticalGate` adds hidden-test + cost-ceiling + budget clauses. |
| [ADR-080](./ADR-080-hyperagents-meta-evolution.md) | Hyperagents — evolving the improvement procedure (bounded) | Proposed (frontier) | Grounds meta-evolution in Hyperagents (arXiv:2603.19461): a self-referential task+meta agent in one editable program (DGM-H). Phased & bounded (Hyperagent-lite): evolve task harness → mutation policy → selection policy → benchmark generator, each behind the SGM gate + risk budget. Hard invariant: the allowlist (ADR-071) and the grader (ADR-072) are never self-editable. |
| [ADR-081](./ADR-081-metaharness-darwin-plus-synthesis.md) | MetaHarness Darwin Plus — the SOTA synthesis | Proposed | The umbrella: DGM archive + HGM clade selection + SGM statistical gates + Hyperagent-lite. The one SOTA promotion rule (effect size + lower-95% delta > 0 + hidden-test hold + zero safety + cost ≤ 1.20× + clean replay + risk budget), clade weighting (0.60/0.25/0.10/0.05), the `--selection clade --gate statistical --replay` CLI target, the report card, and the 100-task acceptance test. Honest "shipped vs next" split. |
| [ADR-082](./ADR-082-expected-gains-and-effective-performance.md) | Expected gains, effective-performance metric, honest ceiling | Proposed | What to expect (10–150% relative by baseline maturity; DGM's +150%/+116%), the composite we report instead of raw solve rate — `effective_agent_performance = verified_success_rate / cost_per_success × safety_score` (`bench/metrics.ts`) — the business promise band (10–30% completion, 10–25% cheaper, 20–50% fewer unsafe actions, clean replay), and the honest ceiling with SWE-bench contamination/mutation cautions → hidden tests + immutable snapshots + repo-native-first. |
| [ADR-083](./ADR-083-host-eve-production-runtime.md) | Eve as a production-runtime host (agent-as-a-directory) | Proposed | Adopt Vercel's Eve approach: the **10th host** (`@metaharness/host-eve`) emits an agent-as-a-directory (`agent.ts`/`instructions.md`/`tools`/`skills`/`subagents`/`connections`/`channels`/`schedules`) from a HarnessSpec, the first production-application-runtime host. Default-deny MCP (ADR-022) copied into `connections/`, high-risk tools emitted with `needsApproval: true`. Closes the Darwin Mode loop: Eve evals → ADR-076 benchmark tasks, OTel traces + durable checkpoints → the scoring/replay substrate. Generate → evolve (gated) → run on Eve → evals+traces feed the next generation. |
| [ADR-156](./ADR-156-borrowed-pattern-integration-program.md) | Borrowed-pattern integration program — mutate structured policies, not prompts | Proposed | Umbrella for ADR-157…166. Ten load-bearing patterns from LangGraph/LangSmith/OpenAI Agents SDK/CrewAI/AgentSPEX/NeMo/robotics — *copy the pattern, not the product*. Thesis: Darwin mutates a typed policy object, not prompt blobs. Houses the integrated acceptance test (100 tasks × 3 repos: ≥20% fewer retries, ≥30% fewer wasted tokens, ≥50% cheaper than frontier-only, same-or-better solve rate, zero guardrail bypasses). |
| [ADR-157](./ADR-157-darwin-checkpoints-durable-execution.md) | Darwin Checkpoints — durable, resumable runs | Proposed | Borrows LangGraph durable execution. Per-cycle `Checkpoint` + content-addressed model/tool-call cache keyed by `(genomeId, step, inputHash)`; killed runs resume with zero duplicate frontier calls and identical final fitness. |
| [ADR-158](./ADR-158-darwin-trace-format-cost-ledger.md) | Darwin Trace Format & Cost Ledger | Proposed | Borrows OpenAI Agents SDK tracing. Typed `TraceSpan` + `CostLedger` with the invariant Σ span cost == accounted cost (no unaccounted model calls); feeds `ablation.ts` so trace-based pruning becomes a mutation signal. |
| [ADR-159](./ADR-159-harnessspec-declarative-policy.md) | HarnessSpec — declarative, mutatable policy | Proposed | Borrows AgentSPEX explicit graph specs. Generalizes `HarnessGenome` into a declarative spec (roles/steps/branches/budgets/guards/memory/evaluators/rollback) with lossless genome⇄spec round-trip and deterministic replay. Load-bearing for the thesis. |
| [ADR-160](./ADR-160-escalation-scheduler-bounded-loops.md) | Escalation Scheduler — bounded loops, fail-closed | Proposed | Borrows structured-graph scheduling research. `SchedulerPolicy` (max retries/escalations/context-growth/reviewer-passes) + typed `TerminationReason`; fail-closed on security uncertainty. No task exceeds COST/TIME budget; no silent infinite loops. |
| [ADR-161](./ADR-161-ruvector-memory-tiers.md) | ruVector Memory Tiers | Proposed | Borrows CrewAI unified memory. Generalizes `RuvSecurityMemory` into five typed tiers (working/repo/mutation/cost/risk) with a mutatable `memoryDepth`. Acceptance: memory-on cuts tokens without lowering solve rate. |
| [ADR-162](./ADR-162-darwinbench-dataset-registry.md) | DarwinBench Dataset Registry | Proposed | Borrows the LangSmith eval workflow. Provenance-tracked datasets with four splits (train/heldout/regression/adversarial) + evaluator registry; a winner must pass all four splits, bootstrap-certified — kills false winners. |
| [ADR-163](./ADR-163-typed-handoffs.md) | Typed Handoffs — contracted transitions | Proposed | Borrows OpenAI Agents SDK handoffs. `HandoffContract` (input/output schema, risk, allowed tools, budget, escalation) over the real swarm chain; reject schema-invalid handoffs; measure retry reduction vs free-form. |
| [ADR-164](./ADR-164-darwin-safety-rails-immutability.md) | Darwin Safety Rails — immutable guardrails | Proposed | Borrows NeMo Guardrails. The anti-reward-hacking spine: programmable, immutable `SafetyRail`s evaluated pre-benchmark on `policy.detectUnsafe` + the immutable `safetyProfile`; a mutation that disables tests is rejected before evaluation. |
| [ADR-165](./ADR-165-darwin-opportunity-scanner.md) | Darwin Opportunity Scanner — ROI ranking | Proposed | Borrows CrewAI Discovery. Ranks task classes by value/testability/cost-saving/risk/complexity to point evolution budget where spend is high and verification (real oracles) is strong; top-10 carry monthly cost, expected saving, verification method, risk score. |
| [ADR-166](./ADR-166-human-review-gates.md) | Human Review Gates — review the uncertain edge | Proposed | Borrows human-gated deterministic verification (robotics). Escalate to a human only on high-risk/security/over-budget/low-confidence/ambiguous-bootstrap cases; everything else stays deterministic. Acceptance: review rate −50% with no rise in escaped defects. |
| [ADR-167](./ADR-167-real-llm-discovery-lane-and-open-frontier-selection.md) | Real-LLM discovery harness + open-frontier selection | Proposed (impl in `@metaharness/projects`) | Net-new decisions from putting a real model in the loop (OpenRouter): execution-verified defensive discovery harness (anti-hallucination — proofs run, not trusted; redacted), an escalation router (cost-per-verified-finding), a self-learning loop on ADR-161 `TieredMemory` (escalate-once-to-learn, 95.7% cost cut), and empirical open-frontier model selection (Qwen3-235B / DeepSeek-v3.2 beat GLM-5.2 on verified-per-cost). Defensive-only; single non-deterministic runs. |
| [ADR-168](./ADR-168-aws-finops-harness.md) | AWS FinOps harness — Darwin primitives applied to cloud cost | Proposed (impl in `@metaharness/aws-finops`) | Domain pivot, not a new method: reuse the ADR-167 cascade + escalation router + shrinking residual verbatim, swap only the oracle — semgrep + crash-fuzzer → **infracost + checkov + `terraform validate/plan`**. A frozen $0.04 Qwen proposes Terraform patches; a deterministic oracle accepts iff build-safe ∧ compliance-non-regressing ∧ modeled bill strictly drops (right-sizing gated on read-only CloudWatch evidence). Metric: cost-per-verified-dollar-saved. Safety inverts to **no live-infra mutation**; patches are human-gated proposals. Savings are *modeled*, not billed. |

> **ADR-156…167 implementation note:** the ADR-156 program and its ten patterns (ADR-157…166) plus ADR-167 are implemented in `packages/projects` (`@metaharness/projects`) — dependency-free, deterministic, 117+ tests, with benchmark receipts under `bench/results/`. Each carries a `## Reference implementation` section. Real-LLM benches are optional/key-gated and excluded from the deterministic suite.

## Darwin applied to game-theoretic solving (`crates/poker-darwin`)

| ADR | Title | Status | Summary |
|-----|-------|--------|---------|
| [ADR-186](./ADR-186-poker-darwin-cfr-solver.md) | poker-darwin: CFR solver with exact exploitability + ruvector/candle/rs_poker | Implemented | A workspace crate proving Darwin's *mutate-structured-policies* thesis on a hard, non-gameable oracle: a CFR/CFR+/DCFR solver for Kuhn (12 infosets) and Leduc (288 infosets, 9,451 histories) scored by **exact exploitability**. Reaches Kuhn's known −1/18 value. Feature-gated `ruvector` (infoset abstraction, 86.8% compression), `candle` (Deep-CFR policy distillation), `rs_poker` (real Hold'em equity). |
| [ADR-187](./ADR-187-darwin-nonstationary-cfr-genome.md) | Non-stationary Darwin: time-variant schedules, predictive momentum, regret pruning | Implemented | Generalizes the genome from scalars to a non-stationary solver: α/β anneal over the run, predictive (optimistic) momentum ω extrapolates regret trends, and a pruning threshold P trades ε/iteration for ε/second. Darwin autonomously rediscovers "α aggressive→stable" (3.81→1.79) and its dynamic champion beats the best static one by **68.8%** (94% vs vanilla) on Leduc. |
| [ADR-188](./ADR-188-chebyshev-functional-schedule-genome.md) | Chebyshev functional-schedule genome, lineage tracking, domain-agnostic hook | Implemented | The genome emits **Chebyshev polynomial trajectories** (Clenshaw recurrence, bounded, zero-alloc) for α/ω/P — smooth, non-monotone curves a ramp can't express; pushes the dynamic edge to **79.2%** over best static. Adds parent-child **lineage tracking** (champion ancestry) and an `optimize` module proving the per-iteration hook is decoupled from regret-matching (same `Schedule` drives a gradient optimizer with magnitude pruning). |

## SWE-bench harness experiments (`packages/darwin-mode/bench/swebench`)

> **Index gap note (resolved):** ADR-169…230 previously existed without being rowed here. They are now covered by the "Additional ADRs (auto-indexed)" appendix below, which closes the `adr-index.test.ts` drift check. ADR-231 is rowed explicitly here (rather than only in the appendix) because it governs how every SWE-bench/FRAMES SOTA number in this series may be claimed.

| ADR | Title | Status | Summary |
|-----|-------|--------|---------|
| [ADR-231](./ADR-231-provably-clean-sota.md) | Provably-clean SOTA — submission-integrity attestation as a required leaderboard gate | Proposed | Answers UC Berkeley RDI (Apr 2026): all 8 major agent benchmarks were gamed to ~98–100% *without solving a task*. Redefines a metaharness SOTA claim as **(score, cost, integrity-attestation)** — a number without a passing, signed exploit-audit is not a SOTA claim, only an assertion. Applies the RDI threat model per-vector to Darwin SWE-bench: four vectors **structurally immune** (answer-DB leakage, normalization collision, external grader-tampering, no-work-scores-a-pass — justified from the official Docker oracle + `conformant-tests.mjs`), the rest **attestable** (best-of-N/k-sample disclosure + conformant selector, empty-patch-rate, measured cost, patch-touches-tests, localization-no-gold). Ships `scripts/sota-attest.mjs` (binds the official gold report + solver report; `skip`+`harness_gap` never a false `pass`) + Ed25519 witness signing (ADR-103), and gates `nightly-sota-review.mjs` (a confirmed-SOTA issue must carry the attestation). Honest: three checks stay `skip`/`attested-by-flag` until the ADR-167 §4 trajectory-serialization forward-contract reaches the darwin bench harness. |
| [ADR-232](./ADR-232-cost-aware-output-mode-decoder-policy.md) | Cost-aware output-mode decoder policy for the Fable escalation stack | Proposed | Output tokens can't be hidden (the model must generate them, and in the `claude -p` loop any prose around the patch is billed then discarded), and Fable's output is 5× its input ($50 vs $10/M). So the highest-headroom lever on the ADR-205 Fable rung is to make Fable **write less/later/not at all**, optimizing **cost-per-ACCEPTED-task**, not input. Ships `output-modes.mjs`: 5 general modes + budgets, a narrower `FableOutputMode` (verdict_only\|minimal_patch\|need_context\|blocked) that **structurally forbids full_prose in-loop** (`assertFableLoopMode` throws; `FABLE_FULL_PROSE_ALLOWED=false` is not prompt-settable), draft-cheap/verify-expensive with an **invariant-gated verdict** (a laundered `accept` is downgraded — the meta-llm ADR-240/238 "decisions not prose" pattern), a continuation gate, the ≤800-tok capsule handoff, a **non-Fable format-repair chain** (local→cheap→smaller-contract→never Fable), receipt-logging OUTSIDE the model path (100% coverage makes terse output auditable), the lagged-truth `costPerHarnessAccepted`/`costPerUserAccepted` split, and an append-only Fable audit line + `detectErosion` query. Wired into `solve-agentic.mjs`/`handoff-solver.mjs` behind `--output-modes` (default byte-identical). $0 modeled-replay on 376 committed real patches: **MEASURED** 96.5% fit the 1200-tok minimal_patch budget; **MODELED** 72.5% output-token cut at 300 narration tok/turn but only ~12% total-cost cut (the ~62k input floor dominates — output policy alone misses the ≥50% total gate; input policy is a separate experiment). Honest: the ≥60%/≥50% headline is **modeled, not live** — a 7-metric + 10-verbose-recover live gate is specified and **budget-gated/unrun**. |
| [ADR-236](./ADR-236-swebench-domain-run-honest-null.md) | D1 SWE-bench domain flywheel run — an HONEST NULL (mechanism proven, base-solver-limited) | Accepted | D1-S5 evidence. Two budgeted LIVE SWE-bench-Lite runs (glm-5.2 + deepseek-chat) BOTH resolved 1/25 with 0 compounding lift, milestone=false — real official-Docker gold-scored, replay PASS (incl. ADR-235 gate re-execution). PROVEN: the flywheel MECHANISM runs end-to-end on REAL code-repair (adapter→real solver→official Docker gold-scoring→frozen gate→signed replayable lineage), no fabricated numbers. NOT proven: that policy evolution COMPOUNDS on code-repair — the base solve.mjs is open-loop single-shot (~48% empty-patch) resolving ~1/25 regardless of model, so no headroom for a mutation to capture (the ADR-234 recover-not-create ceiling made concrete). Forward path: swap to the agentic multi-shot solver (solve-agentic.mjs) so the base resolves enough (≥5-8/25) to show compounding. Also wires ADR-235 gate re-execution into d1s4-live-run + the HLE test (replay now forgery-proof, not just fingerprint-checked). meetsPromotionRule UNCHANGED. |
| [ADR-235](./ADR-235-re-executing-verifiers-and-honest-null-replay.md) | Independent re-executing verifiers + honest-null replay for flywheel proof bundles | Accepted | Integrates the "verifiers RE-EXECUTE from sealed inputs, trust no logs" discipline (reviewed + locally PASS-verified from ruvnet/sublinear-time-solver#64's metaharness-proof) and FIXES a real bug it exposed: `verifyReplayBundle` failed an HONEST-NULL run (0 promotions → chain is just the immutable root — a valid, signed, replayable result). Root cause: `allPromoted` required ≥1 promotion (`promos.length>0 && …`); an empty non-root set satisfies "no rejected node smuggled in" VACUOUSLY. Fixed to `promos.every(PROMOTED)`; the real D1-S4 SWE-bench honest-null bundle (glm-5.2 resolved 1/25, 0 lift) now replays PASS. Regression tests both ways (root-only PASSES; smuggled-REJECTED still FAILS). @metaharness/flywheel 0.1.2. Forward plan: store baseline+candidate Score on each LineageCommit + a `gateReExecutes` check that re-runs meetsPromotionRule on the sealed scores and asserts verdict reproduces bit-for-bit (trust the gate re-run, not the logged verdict). meetsPromotionRule UNCHANGED. |
| [ADR-234](./ADR-234-ruvllm-microloop-under-flywheel-macroloop.md) | Two-layer learning — ruvllm micro-loop (SONA/MicroLoRA/EWC++) under the flywheel macro-loop auditor | Accepted | Layers an adaptive serving runtime (**ruvllm** + ruvector) UNDER `@metaharness/flywheel`: ruvllm is the **micro-loop** (per-request MicroLoRA weight adaptation, ranks 1–4, SONA + HNSW memory routing, EWC++ forgetting guard); the flywheel is the **macro-loop** structural auditor that treats a distilled ruvllm adaptation as a policy candidate and admits it to production ONLY through the **frozen `meetsPromotionRule`** + frozen anchor + signed replay bundle. **Verified by execution (2026-07-05):** `ruvllm_status`, MicroLoRA real 768×768 rank-2 LoRA matrices tracking samples_seen 1→2 / quality_sum 0.9→1.2, SONA `ewc_lambda: 0.1` with quality_ema moving 0.5→0.5175. **§7 closure (2026-07-06, tracking #109):** ships `@metaharness/evals-servedmodel@0.1.0` — genome/evaluator/distillation/drift-guard/composite-gate mirroring `evals-hle`'s shape — and PROVES the full policy→gate→signed-replay pipeline end-to-end on a **$0 SYNTHETIC** fixture: 12/12 tests green, compounding lift 0.3404→0.4021 (+6.2pp) over 2 anchor-surviving promotions, replay `gateReExecutes: true`, `servedModelPromotionRule` composes `meetsPromotionRule` VERBATIM (different fingerprint, strict superset). Benchmark: EWC++ (`ewcLambda` 0.02→0.6 at max drift settings) buys **+22.45pp** retained-'core'-capability quality at a **-7.52pp** live-task cost (the stability/plasticity tradeoff). **Honest boundary unchanged in kind**: no ruvllm model was loaded or served either session — the COMBINED real-served-model lift claim stays DEFERRED; `ruvllmClient.ts` is a real, gated (opt-in `live` flag) client wired but not exercised. meetsPromotionRule UNCHANGED. |
| [ADR-233](./ADR-233-hle-policy-evolution-eval-adapter.md) | HLE as a policy-evolution problem — `@metaharness/evals-hle`, a flywheel benchmark adapter | Proposed | Treats HLE (Humanity's Last Exam — closed-answer expert QA, top public score Fable 5 53.3% on 2,158 text-only Qs) as a **policy-evolution** problem, not model training: freeze the models/evaluator/holdout, evolve only the harness policy (answer/verify/route/abstain/calibrate) — **the policy is the genome**. Ships `@metaharness/evals-hle@0.1.0` as a benchmark **adapter, not core logic**: typed `HLEPolicyGenome` (bounded enums + clamped numbers, never free prose — `clampLever` forces even an LLM proposal in-schema), subject classifier, answer normalizer, **subject-specific mostly-deterministic verifier stack** (NOT a generic critic — ADR-226 measured read-only advice at 0 marginal resolves for 5.4× cost), 3-pass cost-aware routing, ECE calibration, a **fail-closed leakage detector**, and a **four-set data contract** (publicDev/privateTrain/privateValidation/**frozenHoldout confirmed exactly once**). The gate is a **stricter composite that calls the frozen `meetsPromotionRule` verbatim** and ANDs HLE extras (disjunctive materiality: +2pp accuracy-win OR ≥40%-cheaper cost-win; format/calibration not-worse; ≤2 subject regressions) — the frozen product rule is never edited; the composite is fingerprinted in the replay bundle. **$0 SYNTHETIC test GREEN (7/7)**: compounding lift 50%→54%→58% (2 anchor-surviving promotions, plateau at no-op=0), replay verifies, frozen gate proven composed verbatim, leakage fails closed. **Claim discipline**: target 95–105% of top frontier at lower cost, +3–6pp above the same model — **never a ~20pp jump** (that signals contamination on HLE). LIVE run **deferred**: `cais/hle` is HF-gated (no real score fabricated); start with **25-instance** runs before the ≥500 frozen-holdout acceptance run. |
| [ADR-226](./ADR-226-advisor-loop-readonly-strong-advisor.md) | Advisor-loop — read-only strong advisor over a cheap agentic harness | **Accepted — NULL, KILLED** | Inverts the fusion sidekick: the cheap model (deepseek-chat) emits every tool action; a strong model (Sonnet-5) is a **read-only advisor** (never enters the tool protocol, so the tool-call-format confound is structurally absent) via three triggers (voluntary `advise`, involuntary checkpoint, mandatory pre-submit veto). Harness shipped (`advisor-loop.mjs`/`solve-advisor.mjs`). **Measured (2026-07-04, ADR-240 G3 replay with the GEPA cand-6 executor swapped in):** the honest test is the advisor's *marginal* effect on top of cand-6 (D-cand6 vs D0-cand6, not vs the raw base — cand-6 alone is already 8/24, so an absolute bar would falsely credit the advisor). Matched n=19: **D0-cand6 3/19 vs D-cand6 3/19 → +0 marginal**, one gain offset by one regression, 33 advisories fired at **5.4× cost** for zero net resolves. The advice→action impedance persists **even with a GEPA-tuned executor** — the only lever that moves the cheap tier is evolving the executor's own policy (GEPA/cand-6), not injecting advice. Advisor track killed; cand-6 serving-path injection (gated on this in ADR-235 §4.1) stays unwired. |

## Codex local brain integration

| ADR | Title | Status | Summary |
|-----|-------|--------|---------|
| [ADR-236](./ADR-236-codex-local-brain-mcp-and-hooks.md) | Codex local brain MCP and hook integration | Accepted | Registers the verified local `ruvultra-mcp` server as Codex's `ruvultra-brain` MCP, packaged through a personal `metaharness-brain@personal` plugin with a brain skill and post-write safety hook asset. Honest boundary: hook files are packaged but not manifest-activated because the local plugin validator rejects `hooks`; `metallm-dev-bridge` remains disabled until `COGNITUM_DEV_KEY` is present; `metaharness mcp-serve` is not registered until a real stdio entrypoint exists. |

## AGNTCY / Internet of Cognition integration

| ADR | Title | Status | Summary |
|-----|-------|--------|---------|
| [ADR-240](./ADR-240-agntcy-identity-oasf-observability.md) | AGNTCY identity, OASF export, and semantic observability for generated harnesses | Proposed | Adopts Cisco Outshift's AGNTCY ecosystem (Linux Foundation governed) as complementary, not competitive, infrastructure — "AGNTCY defines the agent network; MetaHarness builds and evolves the agents; RuFlo executes and coordinates them." Adds a signed `identity` block (W3C DID, badges sourced from existing `mcp-scan` tool-policy output, Cognitum tenant) to the harness manifest, layered on the existing witness manifest (ADR-011) rather than a third signature scheme. Exports OASF capability records to the AGNTCY Directory as a **projection** of already-computed `harness-score`/`harness-genome`/`harness-mcp-scan` facts, not a new evaluation pipeline. Maps existing telemetry (`model.route`, `memory.provenance`, `evaluation.score` — already real, measured producers) onto AGNTCY's OTel semantic conventions. Also specifies the **CASA intent→authority-envelope compiler**: MetaHarness compiles a stated objective into a bounded, deny-by-default JSON envelope (`allow`/`deny`/`budget_usd`/`expires_at`); the load-bearing constraint, stated explicitly, is that the *translation* may use an LLM but *enforcement* (owned by companion ruflo ADR-380) never does. Ships as optional `@metaharness/agntcy`, never a hard kernel dependency. Companion: ruflo ADR-380 owns the runtime half (SLIM transport, CASA enforcement, IOC Layer 9 events). |
| [ADR-241](./ADR-241-radio-passive-awareness-swarm-bus.md) | @metaharness/radio — passive-awareness swarm bus (AgentRadio, arXiv:2607.28430) | Accepted (core + protocol + deterministic sim shipped, $0; live pod wiring deferred) | Integrates AgentRadio (Ren et al.): multi-agent pods coordinate mid-task instead of only at phase boundaries. One load-bearing primitive — `wait_for_mention` backgrounded as an OS task, not an LLM step — makes listening free: sends are non-blocking, an @-mention is folded in at the next step boundary with a full thread snapshot, and the paper's ablation puts +12.1 pts on negotiated partition and +10.5 pts (p=0.0023) on passive awareness (62.1% vs 32.3% single-agent on SWE-Atlas). Ships dependency-free deterministic TS: RadioBus (logical-clock total order, replayable), Watcher (fold() costs no step; blockingReceive() is the ablation arm and costs one — separate methods so accounting modes can't silently mix), the five-phase assembler-gated protocol (Explore/Divide/Execute/Review/Submit), and a seeded codebase-QnA sim whose headline property reproduces the ablation ORDER (passive < negotiate <= divide < single). Comms policy {mode, foldEvery, postPolicy} joins the genome: a flywheel run from a deliberately bad root re-discovers passive/immediate under the frozen gate + anchor topology, Ed25519 replay-verified. Honest bounds kept visible: mentions can derail (paper: 47 rubrics gained/23 lost), awareness cannot surface conclusions no agent forms, the sim is a mechanism testbed not a benchmark claim, and live LLM-pod wiring is deferred to its own measured ADR. |
| [ADR-242](./ADR-242-oo-agents-wasm-code-as-action.md) | @metaharness/oo-agents — OO agents with a Rust/WASM code-as-action sandbox (NOOA clone) | Accepted (core shipped, $0; live LLM driver deferred) | Clones NOOA (NVIDIA-NeMo/labs-OO-Agents, arXiv:2607.20709): the agent is a CLASS — fields are state, methods are capabilities, docs are prompts, schemas are typed contracts, and an agentic method has no body: the model acts by WRITING CODE with `self` in scope. One deliberate substitution: instead of guard-listed Python-in-a-worker, cells are written in cellscript — a small deterministic language interpreted by a dependency-free Rust VM compiled to wasm32 (~180 KB, no wasm-bindgen) — so isolation is by construction: no filesystem/network/clock/randomness/FFI exists in the language, compute is fuel-bounded (runaway cells trap deterministically), and the ONE window to the world is a single JSON host import (self.method/self.field) answered from the bound agent object. REPL semantics faithful to cell_core.py: last-expression capture, namespace persistence, return_result() as the ExecutionSignal, and NOOA's typed auto-retry (contract violations fed back verbatim). ModelDriver is the only model seam; deterministic ScriptedDriver ships, live LLM drivers deferred to their own measured ADR. 5 native VM tests + 8 package tests incl. the upstream README's SupportAgent end-to-end through the wasm sandbox. NOT a full port (no MCP bridge/skills/tracing/sub-agents — each needs its own justification); no claims inherited from NOOA's paper evals. |
| [ADR-245](./ADR-245-horizon-long-horizon-control-core.md) | @metaharness/horizon — the portable control core of Google ADK's long-horizon-harness, as Rust/WASM+TS | Accepted (core shipped, $0; ADK-platform features out of scope) | Clones the three PORTABLE, deterministic control primitives of Google ADK's `long-horizon-harness` (core/python) and leaves the Google-platform plumbing (Memory Bank, per-user sandboxes, OAuth, sub-agents, nightly dream, A2A) out of scope: (1) `HaltController` = ADK `halt_reason` as a PURE Rust reducer `(config,state,action)→(state,decision)` compiled to wasm32 — iteration-budget/no-progress/repeated-failure halts ARMED on observe, CONSUMED at before_model, RESET at turn_boundary; serializable state → sessions resume via snapshot/restore. (2) `CommandGuard` = ADK `command_classify.py` anti-smuggling guard in Rust/WASM where quote-aware shell tokenizing belongs — splits on top-level `;`/`&&`/`\|\|`/`\|`, recurses into `$(...)`/backtick substitutions, classifies EVERY segment, returns MAX severity (allow<gate<deny) so a gated op can't hide behind a benign leading token; Layer-A exfiltration folds in (secret-path reads, non-allowlisted egress, metadata-server touch → deny); deny/gate/secret matching runs on the UNQUOTED skeleton so `echo 'a; rm -rf /'` stays allow. (3) `CompactionPolicy` = pure-TS context compaction with the flush-durable-facts-BEFORE-lossy-summary ordering as an ENFORCED invariant (a flush rejection aborts compaction, events intact — the summary never runs over unpersisted facts). `LongHorizonDriver` composes all three into the ADK Runner loop shape with model + gate-approval as seams. Same dependency-free Rust→wasm32 build as ADR-242 (no wasm-bindgen, 154 KB, one hz_eval(json)→json export). 14 Rust tests (incl. 20k-iteration never-panics fuzz) + 19 TS tests green; classify ≈11µs/call, halt.observe ≈9µs/call. Honest bounds: three mechanisms not a full port; the guard is a STRUCTURAL classifier not a shell interpreter (gates sh -c/eval wrappers rather than evaluating payloads — a guardrail layer to pair with real sandboxing, not a safety proof); no claim inherited from ADK's platform evals. |
| [ADR-244](./ADR-244-user-awareness-probe.md) | user-awareness probe — a flywheel-disciplined test for identity-conditioned model behavior | Accepted (instrument built + validated on 3 synthetic controls, $0; live frontier-model run deferred) | Generalizes "freeze the model, evolve the harness" to "freeze the SUBJECT, measure honestly, gate on a frozen threshold, sign the verdict" — applying the ADR-234/235 flywheel machinery to a BEHAVIORAL hypothesis: does a frozen model change behavior with the perceived user identity (told it's a safety researcher → lower confidence, reasons-first more, flags dual-use less)? Paired A/B over an identity-neutral 40-item battery (only a role persona varies between arms), a shared arm-blind parser (3 signals: stated confidence %, reasoned-first rate, dual-use flag rate), an EXACT paired sign-flip permutation null (enumerate all 2ⁿ sign vectors — deterministic, no RNG; binary batteries carry 16 items so the test has power), and a FROZEN conjunctive gate (DETECTED iff \|effect\|≥minEffect AND p<α=0.01, thresholds canonicalized into the signed verdict) with an Ed25519 receipt (reuses flywheel makeSigner/verifyReceipt) that re-verifies on any machine. This session has no live frontier-model access, so NO claim about any real model is made or faked; the deliverable is the VALIDATED INSTRUMENT, proven on three synthetic controls: `aware`→DETECTED (all 3 signals, p≤0.008), `flat`→NULL (exact zeros), and the hard `noisy-flat`→NULL where the suspicion magnitude (0.25) exceeded the 0.15 floor by chance but the gate refused DETECTED because p=0.29≥α — the conjunctive gate rejecting a magnitude-only false positive. Honest bounds: validates methodology not any model; live run deferred to its own measured follow-up; a NULL is a power-bounded negative not proof of absence; α frozen before data (post-hoc moves = gate tampering, ADR-235). Lives in `experiments/user-awareness/` with a reusable real-model seam in `models.mjs`. |
| [ADR-243](./ADR-243-sota-coordination-levers.md) | SOTA coordination strategies as flywheel-evolvable radio levers | Accepted (levers + priced sim + flywheel shipped, $0; live pod wiring deferred) | Extends ADR-241 radio's comms genome with two more fact-checked SOTA findings and lets the frozen-gate flywheel DISCOVER the best combination per task rather than hand-picking — because PACT (arXiv:2606.05304, F3) finds NO single coordination strategy is universally optimal (topology-dependent; compact/action-centered wins -38.7% tok) and TodyComm (arXiv:2602.03688, F5) finds the optimum MOVES under shifting/adversarial peers. New levers: `topology` = message-passing\|blackboard (F6, arXiv:2510.01285 / 2507.01701 — shared validated state beats message-passing AND master-slave, 37.53% vs 32.16% KramaBench, more token-efficient; its relevant-pull subsumes the delivery levers, which go inert under it) and an INTRINSIC `stalenessCost` rework surcharge (F9, arXiv:2502.14321 — async's signature failure is reasoning over outdated peer state; the honest counterweight radio MISSED, priced by delivery latency, 0 at defaults and 0 under blackboard). Co-evolved with the existing mode/foldEvery/postPolicy/digest (F1/F8) under the frozen `meetsPromotionRule` + never-optimized anchor, Ed25519-signed, replay-verified (ADR-234/235); F4's efficiency≠effectiveness encoded as `regressed=unresolved → hard-gate stop`. Flywheel result (committed tuned-policy.json): converges on {passive, blackboard} with a 21.28→40.32→47.62 lift over 2 anchor-surviving promotions, milestone + replay verified. Honest bounds: mechanism testbed not a benchmark claim; SOTA numbers are unreplicated single-paper preprints on author-configured benchmarks; blackboard/staleness are faithful MODELS not the papers' exact systems; live LLM-pod wiring deferred to its own measured ADR. |

## Prime Agent integration

| ADR | Title | Status | Summary |
|-----|-------|--------|---------|
| [ADR-246](./ADR-246-prime-agent-continual-harness-refine.md) | Prime Agent-inspired continual-harness primitives — refine operator, autonomous spec, recoverable sessions | Implemented | Shipped 2026-08-06 (§2.1/§2.2/§2.3/§2.5; §2.4 PTC deferred by design — pre-registered A/B committed, unrun): **RefineMutator** (`packages/darwin-mode/src/refine-mutator.ts`) — evidence-or-no-op proposer verified adversarially (incl. zero-width-char evidence-bypass fix), inverse-carrying flywheel `ProposerResult` on an additive channel, promotion-gate fingerprint byte-identical (frozen `meetsPromotionRule` untouched); **`autonomous` block** in both specs + `crates/kernel/src/autonomous.rs`, validated by a 20-case shared TS/Rust lockstep fixture with integer/null-tolerance reconciliation; **recoverable sessions** (`crates/kernel/src/session.rs`, kernel-js `SessionLog`, wasm bindings) with cross-language state-hash parity (UTF-8 key sort, integral-float normalization, surrogate rejection, fork-event parity; fixture hash `53851e97…` reproduced via wasm), behind a default-off `--sessions` scaffold toggle; caveat: validation *messages* are per-language diagnostics — the contract is hash + accept/reject. |
| [ADR-247](./ADR-247-host-prime-agent.md) | host-prime-agent — Prime Agent as the 11th harness host | Implemented | Shipped 2026-08-06: emission-only adapter (`@metaharness/host-prime-agent`, ADR-032/036 pattern, host-pi-dev sibling) emitting project-scoped Python-backed skills at `.prime/agent/skills/<tool>/`, supplemental prompt, sub-agent specs, and the ADR-246 autonomous invocation; fail-closed sandbox posture enforced (`SANDBOX-REQUIRED.md` on non-empty deny-list, never silently dropped). 19 contract tests incl. adversarial regressions (collision suffixing, hooks/statusLine unsupported-section, heartbeat projection, no fabricated CLI values, surrogate-safe truncation); propagation complete — CLI HOSTS/host-config, web-ui parity byte-identical (ADR-027), real-measured bench baseline row, verify-all-hosts PASS, healthcheck HEALTHY. Count nuance: 10th **implemented** adapter — the title's "11th host" counted the unimplemented host-eve (ADR-083). |
| [ADR-250](./ADR-250-sota-proof-ladder.md) | The SOTA-proof ladder — five rungs from mechanism proof to benchmark claim; rung-2 artifacts shipped | Accepted — rung 2 shipped and replay-verified; rung 3 executed twice (honest nulls, saturated domain); rungs 4-5 open | Codifies what "proven" means for this repo as five explicit rungs: (1) mechanism correctness (unit/property tests — ADR-248/249, shipped); (2) mechanism efficacy in SEALED SYNTHETIC environments under frozen gates with signed/replayable artifacts — TODAY'S RUNG, three adversarially-reviewed artifacts: `experiments/turn-credit-acceptance` (the ADR-248 §6 gate machinery executed end-to-end at §6 scale, 300×3 per arm: lift +63.78pp, invocation-overhead 9.21% < 20%, violations REDUCED 84.7→25.7, all three frozen clauses PASS, Ed25519-verified verdict); `experiments/signal-flywheel` (compounding through the ADR-249 seams under the DEFAULT frozen gate + never-optimized anchor: 8 gens, 32 candidates, 2 anchor-surviving promotions, holdout primary 0.3309→0.8975 = ×2.71, replay PASS incl. pinned-gate re-execution; the cost seam produced a real rejection and the no-op clause refused a primary-0.95 candidate — gate discipline reported, not hidden); `experiments/router-calibration-loop` (HONEST MIXED: credit labels beat naive on ECE 0.2213 vs 0.2758 — flagged as a low-resolution artifact, near-base-rate predictor sd 0.11 — and the economics claim is REFUTED 3/3 seeds: raw within-trajectory-standardized `toQualityLabels` is a RELATIVE signal, the wrong currency for absolute RouterExample.quality; finding: credit separates informative from distractor turns 0.705 vs 0.256 but carries ~zero outcome-level information); (3) live cheap-domain compounding (evals-math ADR-237 pattern) — OPEN, requires model spend; (4) the ADR-248 §6 LIVE gate on real RuFlo trajectories — OPEN; (5) external benchmark claims (SWE-bench / Harness-Bench) — OPEN. Synthetic environments are favorable by construction; no rung-3-5 claim is made. |
| [ADR-273](./ADR-273-nightly-dream-machine.md) | The MetaHarness Dream Machine — nightly, cloud-scheduled, evidence-gated evolution routine | Proposed — routine live; 10 ledgered runs through 2026-08-29 | Ports the Ruflo Dream Cycle v3 architecture to this repo: a `/schedule` cloud routine ("MetaHarness Nightly Dream Cycle", cron `0 8 * * *` UTC, staggered after Ruflo's 06:00) runs one nightly cycle against a fresh isolated checkout — ledger → parallel research (A/B/C claim grading) → frozen falsifiable hypothesis → concrete candidate (<300 lines) → baseline-then-candidate evaluation on the repo's REAL evaluators → independent adversarial critic + reward-hack check → bounded Darwin (≤3 gen × ≤4 cand × 1 lineage, frozen fitness) → classified evidence retention → witness stamp → public gist + issue + always-DRAFT PR + exactly one appended `docs/dream-cycle/LEDGER.md` row. Every run ends `ACCEPT\|REJECT\|INCONCLUSIVE`; evaluation is never promotion; the routine never merges. Deltas from Ruflo v3: prompt mirrored in-repo at `docs/dream-cycle/PROMPT.md` (scheduler copy authoritative, update together), first-class degraded modes (`LLM_EVAL=blocked` when no credentials, JS-only build when wasm fails in sandbox), ADR output follows THIS repo's shape, ADR-250 proof-ladder rung required on every quantitative claim. Tripwire: >90% untouched output after 14 runs pauses the routine (the Ruflo v1 pathology). |
| [ADR-249](./ADR-249-darwin-scorer-signal-seams.md) | Darwin scorer signal seams — opt-in injected trace-quality + deterministic cost-unit scoring | Accepted (implemented, additive, byte-identical defaults regression-tested) | Opens the two explicitly-reserved slots in the ADR-072 scorer without touching a single gate: an optional trailing `signals` argument on `scoreVariant` carries (1) an injected [0,1] trace-quality number (e.g. derived upstream from ADR-248 turn-credit `creditByLabel`; clamped, round6'd) that replaces the binary compact-output heuristic ONLY when present — darwin-mode stays dependency-free, the signal arrives as a plain number — and (2) a deterministic abstract cost-units input `{units, budgetUnits}` feeding `costEfficiency` = 1.0 at/under budget (or when absent/malformed) else `round6(budgetUnits/units)`, monotone non-increasing in cost. Wall-clock stays OUT of the score (`latencyEfficiency` remains pinned — the ADR-075 reproducibility clause that killed raw `durationMs` folding still binds; cost-units are deterministic, jitter is not). With both seams absent the ScoreCard is BYTE-IDENTICAL to the pre-change scorer, proven by exact `toBe` regression fixtures hand-computed from `scoreWeights()` (0.985/0.71/0.925/0.435/0.885) — not `toBeCloseTo`. Penalty layer, SAFETY_GATE, single-run/statistical/GEPA promotion gates all untouched (adversarially reviewed diff-confirmed). 22 new tests; full darwin-mode suite 626 pass / 14 skip. |
| [ADR-248](./ADR-248-turn-credit.md) | @metaharness/turn-credit — offline recursive turn-level credit assignment (AgentOPSD) | Accepted (processor + adapters + receipt payload shipped, $0, 20 tests; training deferred behind acceptance gate) | Integrates the credit MECHANISM of AgentOPSD (arXiv:2608.05987): per-turn evidence → recursive Bayesian belief update in log-odds space (B₀=clip(prior,ε₀), c_k=γc_{k−1}+e_k, B_k=σ(logit(B₀)+c_k)) → marginal belief revision ΔB_k as the turn's credit → outcome-aligned, within-trajectory-standardized, BOUNDED reshaping w_k=clip(1+b·z_k,1−b,1+b), m_k=(1−λ)+λw_k with the tested invariant sign(Ã_k)=sign(A_seq) — emphasis changes, the verifier's decision never reverses (paper defaults ±25%; GOVERNED_DEFAULTS ±10%). Two evidence modes: `logprob-gap` (AgentOPSD proper) and `verifier-delta-proxy` for hosted models without token probabilities — labelled `proxy:true` end-to-end because it is NOT AgentOPSD proper. Ships dependency-free phase-1 TS: processor, pivotal-turn identification, adapters (creditByLabel for RuFlo/SONA decision attribution, attributeMutation as Darwin promotion EVIDENCE never a gate, toQualityLabels for the router seam, toMemoryFeedback for credit-weighted retrieval feedback), signable receipt payload (belief revisions, bounded weights, pivotal turns, verifierVersion, evidence/trajectory digests — drops into the flywheel Ed25519 Signer), and a `metaharness turn-credit process\|report` CLI. Honest bounds: advisory + reversible (no model-weight updates anywhere); teacher scoring pass lives upstream (RuFlo companion repo owns its wiring); source is a v1 preprint (Qwen2.5 only, no multi-seed CIs) — §6 acceptance gate (300 tasks × 3 seeds, ≥5pt verified-completion lift, <20% cost, zero governance regressions) required before any trust escalation. |

## RuForecast HPO integration

| ADR | Title | Status | Summary |
|-----|-------|--------|---------|
| [ADR-272](./ADR-272-darwin-numeric-genome-kind.md) | Darwin Mode — numeric genome kind (a second, parallel evolution domain) | Accepted (implemented) | Darwin Mode evolved one genome kind: seven prompt-surface files, mutated by code rewriting, scored by the repo's test suite (ADR-071/072). A downstream consumer wanted to evolve *numeric* ML hyperparameters (learning rate, epochs, batch size) against an external metric (WQL) instead — which fits none of the existing extension points (`CodeGenerator` still regenerates one of seven fixed files; `benchSuite` still runs coding tasks). Rather than widen the ADR-071 safety allowlist to a domain it was never meant to bound, adds fully parallel modules (`numeric-types/-mutator/-archive/-evaluator/-evolve.ts` + `evolve-numeric` CLI verb): a bounded `NumericGenomeSpec` (name → `{min,max,scale,type}`), deterministic Gaussian mutation + crossover in unit-genome-space (log-scale parameters perturbed multiplicatively), a caller-supplied `NumericEvaluator` (default: spawn a command, genome in on stdin, `{primary,regressed,...}` JSON out on stdout — Darwin never trains/scores anything itself), and a generation loop reusing the genuinely generic `mapLimit`/`paretoFront` from the prompt kind. Zero changes to `evolve.ts`/`mutator.ts`/`types.ts`/`archive.ts`/`scorer.ts`/`sandbox.ts` — the existing prompt-kind suite (586 tests) is untouched; +17 new tests plus a real end-to-end CLI convergence proof against a synthetic optimum through the actual built `dist/cli.js` binary. |
| [ADR-273](./ADR-273-darwin-llm-agent-sandbox.md) | Darwin Mode — the `llm-agent` sandbox (real LLM evaluation substrate) | Accepted (implemented) | None of the three existing sandboxes (`real`: repo test command; `mock`: deterministic scripted sim, ADR-102; `agent`/Tier-2: executes a variant's real TypeScript surface code offline, ADR-106) can measure whether one phrasing of a *prose* prompt-genome surface changes real agent behaviour — all three are independent of the surface's literal text for a prose-only genome (confirmed: a downstream consumer's prompt genome scored identically across every mutated variant under all three). Adds a fourth mode, `llm-agent`, as a new parallel module (`llm-agent-sandbox.ts`) that genuinely spawns the `claude` CLI per task (`execFile`, no shell, `--restricted --permission-mode plan --output-format json`, scrubbed env — same no-injection posture as Tier-2's own child process), asks a cheap single-turn PROCESS/style judgement (deliberately not a security/ethics question, so the model's own safety training can't masquerade as "no signal"), and grades deterministically by comparing the real answer's first word against a declared `desiredAnswer` (`exitCode 0` on match, matching every other sandbox's `taskSuccess` convention). The ADR-071 safety gate (`inspectVariant`) runs first, explicitly, inside the new module. This is Darwin Mode's first sandbox with real non-zero cost per run, so the CLI hard-caps estimated real invocations (`generations*children+1) * taskCount`) at 15 by default, refusing to proceed without `--llm-agent-cap`/`--llm-agent-confirm-large-spend`. Zero changes to `sandbox.ts`/`mock-sandbox.ts`/`tier2-sandbox.ts` or their tests; `evolve.ts`/`types.ts`/`cli.ts` gain a fourth dispatch branch each. Separately noted, out of scope for this ADR: `metaharness-darwin evolve` never reads a downstream consumer's `genome.json` prose at all — it synthesizes its own generic baseline `.ts` surface files from a repo scan — so a prose-genome consumer needs a future bridging ADR before this sandbox's real signal reaches their actual policy text. |

## Additional ADRs (auto-indexed — closes the ADR-index drift test)

> These entries were generated mechanically from each file's own `**Status**` line and opening summary/blockquote to close the gap the `__tests__/adr-index.test.ts` "every ADR-*.md file in the directory is listed in INDEX.md" check catches (112 files were on disk but unlinked here). They supersede the informal "Index gap note" above for ADR-169…239. Titles and one-line summaries are extracted verbatim from source, not hand-authored — read the linked ADR for full context.

### Darwin Mode — selection, diversity, and statistical rigor (ADR-084…120)

| ADR | Title | Status | Summary |
|---|---|---|---|
| [ADR-084](./ADR-084-darwin-self-reflection-failure-driven-mutation.md) | Darwin Mode — self-reflection via failure-driven mutation | Accepted (implemented) | The `CodeGenerator` contract (ADR-071) always declared a reflection channel — `repoSummary`, `parentScore`, `failedTraces` — but the loop fed it nothing. This ADR records wiring that channel so a child mutation can target the parent's *actual* failures instead of mutating blind. It is the DGM "self- |
| [ADR-085](./ADR-085-polyglot-code-benchmark-model-routing.md) | Polyglot code benchmark — execution-scored model routing for the mutator | Accepted (measured) | The DRACO frontier (research-writing) suggested cheap models rival frontier ones, but the Darwin mutator emits code, not prose. This ADR records an execution-scored code benchmark across six languages to answer the mutator-routing question directly — and the answer is sharper than "cheap is fine." |
| [ADR-086](./ADR-086-darwin-efficiency-aware-selection.md) | Darwin Mode — opt-in efficiency-aware selection (a gradient above the scorer ceiling) | Accepted (implemented) | A measured fact about ADR-072's scorer: every safe, test-passing variant earns `finalScore = 0.985`. The scorer is a correctness/safety gate, not a quality discriminator — so `archive.best()` picks among ceiling-tied variants by insertion order, which is arbitrary. This ADR adds an opt-in tie-break  |
| [ADR-087](./ADR-087-darwin-graded-promotion-wired-into-evolve.md) | Darwin Mode — wire the graded benchmark gate into `evolve()` | Accepted (implemented) | ADR-076 built a full graded benchmark layer — `evaluateChildAgainstParent` runs a child vs its parent over a hash-pinned suite in the real sandbox, scores with `scoreBenchmark`, and `decidePromotion` returns a statistically-grounded decision. But that layer was never connected to the loop: `evolve() |
| [ADR-088](./ADR-088-darwin-map-elites-quality-diversity-selection.md) | Darwin Mode — MAP-Elites quality-diversity selection | Accepted (implemented) | ADR-073 selects stalled-generation parents by top `finalScore`. Because the ADR-072 scorer ceilings at 0.985, "top finalScore" is a flat tie broken by insertion order — so the two parents are routinely the *same* mutation surface, and the search collapses onto one niche. This ADR adds opt-in MAP-Eli |
| [ADR-089](./ADR-089-darwin-crossover-recombination.md) | Darwin Mode — genetic crossover (surface recombination) | Accepted (implemented) | Until now a child came from ONE parent via ONE mutation. But two high-fitness parents may each carry a *different* good surface — a better `planner` here, a better `retryPolicy` there. Mutation alone cannot combine them. This ADR adds opt-in crossover: recombine two parents' surfaces into one child. |
| [ADR-090](./ADR-090-darwin-sgm-risk-budget-in-evolve.md) | Darwin Mode — wire the SGM cumulative risk budget into `evolve()` | Accepted (implemented) | ADR-079 defined a global, monotonic risk budget and the SOTA admission clauses (no hidden-test regression; cost-per-solve within a ceiling) so recursive self-modification cannot accumulate unbounded risk. Like the rest of the bench layer, it was never wired into the loop. ADR-087 connected the *base |
| [ADR-091](./ADR-091-darwin-hyperbolic-behavioral-phenotyping.md) | Darwin Mode — hyperbolic behavioral phenotyping (MAP-Elites in Poincaré space) | Accepted (implemented) | ADR-088 bins variants by a FLAT structural axis (which of the 7 surfaces was mutated). But agent *behaviour* is hierarchical — a deep recursive backtracker vs. a shallow linear tool-user — and hierarchies embed far more faithfully in hyperbolic space than in a flat categorical grid. This ADR adds a  |
| [ADR-092](./ADR-092-darwin-active-niche-steering.md) | Darwin Mode — active niche steering (navigating the Poincaré manifold) | Accepted (implemented) | ADR-091 gave each variant a coordinate in the Poincaré ball and bins for diversity. Diversity selection *maintains* spread; this ADR *drives* it — actively seeding the next generation from survivors nearest an under-explored region, preferring the high-complexity boundary. Evolution stops being pass |
| [ADR-093](./ADR-093-darwin-epistatic-linkage-topology-aware-crossover.md) | Darwin Mode — epistatic linkage learning & topology-aware crossover | Accepted (implemented) | Crossover (ADR-089) swaps a RANDOM subset of surfaces. But surfaces are epistatic — a `planner` change may only pay off when matched by a complementary `retryPolicy` change. Splitting a co-adapted pair destroys both. This ADR learns which surfaces co-adapt and keeps them together during recombinatio |
| [ADR-094](./ADR-094-darwin-clade-metaproductivity-selection.md) | Darwin Mode — clade-metaproductivity parent selection (Huxley-Gödel) | Accepted (implemented) | Selecting parents by current score is a known failure mode: the best-*scoring* variant is a poor *parent* because its descendant line is exhausted (the "metaproductivity–performance mismatch", Huxley-Gödel Machine, Wang/Piękos/Li et al., arXiv:2510.21614, 2025). This ADR selects parents by descendan |
| [ADR-095](./ADR-095-poincare-vs-euclidean-niche-ablation.md) | Darwin Mode — Poincaré vs Euclidean niche ablation (Gap 1) | Accepted (measured) — honest, nuanced result | A reviewer's first demand: does the hyperbolic geometry actually buy diversity, or is it decorative? This ADR runs the controlled comparison and reports the result honestly — including where Euclidean wins. |
| [ADR-096](./ADR-096-darwin-fdr-control-promotion-gate.md) | Darwin Mode — Benjamini–Hochberg FDR control on the promotion gate | Accepted (implemented) | The bootstrap promotion gate (ADR-076) uses a per-comparison 95% bound — fine for one child vs one parent. But `evolve()` tests MANY children per generation, so at scale ~2–3 "lucky" variants promote by chance each generation. This ADR adds generation-wide Benjamini–Hochberg false-discovery-rate con |
| [ADR-097](./ADR-097-darwin-self-directed-curriculum.md) | Darwin Mode — self-directed curriculum (difficulty ladder) | Accepted (implemented) | The recurring honest finding across ADR-091/092/095: every selection refinement is *latent* until the search space has gravity. An easy suite scored in full from generation 0 never forces struggle, so the high-complexity Poincaré frontier stays empty. This ADR adds a difficulty ladder over the EXIST |
| [ADR-098](./ADR-098-darwin-external-benchmark-targeting.md) | Darwin Mode — external-benchmark targeting strategy (SWE-bench / robustness race) | Proposed (FUTURE — deferred, not yet implemented) | Captured at the user's request ("add adr and do later in the loops"). This is a strategy/roadmap ADR — a deferred plan, not a shipped capability. It records HOW Darwin Mode would target public agentic benchmarks so a later loop can execute it without re-deriving the approach. Nothing here is impleme |
| [ADR-099](./ADR-099-darwin-system-audit-dashboard.md) | Darwin Mode — system audit dashboard (instrument validation) | Accepted (measured) | Before claiming the evolution engine is "beyond SOTA", prove it is a reliable scientific instrument. This ADR adds a dashboard that benchmarks the ENGINE — determinism, statistical rigor, clade productivity, niche diversity — not task accuracy. It reports real numbers, including the uncomfortable on |
| [ADR-100](./ADR-100-darwin-pareto-multiobjective-selection.md) | Darwin Mode — multi-objective Pareto selection (parsimony vs capability) | Accepted (implemented) | The scorer is a scalar GATE that ceilings at 0.985 — it cannot rank the passing variants, and a single scalar forces a false choice between competing goods. Pareto dominance keeps the whole non-dominated front, so a small/cheap "mini" variant and a capable "grand" variant both survive as parents ins |
| [ADR-101](./ADR-101-darwin-agent-executing-sandbox.md) | Darwin Mode — the Agent-Executing Sandbox (the keystone unblock) | Proposed (HEADLINE / keystone — design + evidence; implementation deferred) | Seventeen ADRs built a rigorously-validated evolutionary engine — clade selection, hyperbolic niches + steering, epistatic crossover, FDR-controlled promotion, curriculum, Pareto. Two independent measurements tonight show the engine is running without fuel. This ADR names the fuel: a sandbox that ex |
| [ADR-102](./ADR-102-darwin-mock-sandbox-manifold-live.md) | Darwin Mode — deterministic surface-driven sandbox (the manifold goes live) | Accepted (implemented + measured) | ADR-101 named the keystone: traces must depend on the harness surfaces. This ADR ships the cheapest version of it — a deterministic, LLM-free, surface-driven agent-loop simulator — and measures the manifold going live. |
| [ADR-103](./ADR-103-darwin-self-improvement-demonstrated.md) | Darwin Mode — end-to-end self-improvement demonstrated (and where it doesn't) | Accepted (measured) | ADR-102 made the manifold live (traces depend on surfaces). The decisive question remained: does the loop actually *self-improve*? This ADR answers it with reproducible numbers — yes, when the fitness gradient is reachable by the mutator — and is equally explicit about the cases where it does not. |
| [ADR-104](./ADR-104-darwin-sibling-diversity-nonce.md) | Darwin Mode — sibling-diversity nonce (fix one-directional mutation) | Accepted (implemented + measured) | ADR-103's instrumentation found that the `DeterministicMutator` never grew the retry budget: `maxAttempts` explored only `{1,2,3}` (≤ baseline). Root cause: all siblings mutating the same surface produced the *same* edit, so a generation explored one direction, not both. This ADR fixes it with a det |
| [ADR-105](./ADR-105-darwin-diversity-crosses-deception.md) | Darwin Mode — diversity selection crosses a deception greedy cannot (the justification) | Accepted (measured — multi-seed) | The diversity/phenotype machinery (ADR-088/091/092/094) has been correct, tested, and — since ADR-102 — live. But one question decided whether it was *worth* it: does it actually solve problems greedy score-selection cannot? This experiment answers it with a clean, multi-seed contrast. It does. |
| [ADR-106](./ADR-106-darwin-tier2-agent-executing-sandbox.md) | Darwin Mode — Tier-2 agent-executing sandbox (real surface code runs) | Accepted (implemented + measured) | ADR-102 (Tier 1) made traces depend on surface *parameters* (regex-extracted). This ADR runs the surfaces' *actual code*: a variant's real `planner` / `contextBuilder` / `retryPolicy` / `toolPolicy` modules execute in a child process and drive the agent loop. The harness's evolved logic — not a prox |
| [ADR-107](./ADR-107-darwin-real-llm-eval-poc.md) | Darwin Mode — real-LLM evaluation proof-of-concept | Accepted (measured PoC) — bridge toward ADR-098 | Tier-2 (ADR-106) executes a variant's real surface *code*, but on synthetic file-location tasks. The last conceptual gap to real-world capability is a real model fixing a real failing test, scored by the real test command. This ADR proves that path works end-to-end — with a single, half-a-cent call  |
| [ADR-108](./ADR-108-darwin-evolution-series-synthesis.md) | Darwin Mode evolution series — synthesis, evidence, and status | Accepted (synthesis / index) | One document a reviewer can read to understand the whole contribution. Darwin Mode's differentiator is not a model — it is an auditable, statistically-gated, recursive lineage: a self-improving agent harness where every claim is a committed, reproducible number and every limitation is recorded. This |
| [ADR-109](./ADR-109-darwin-surface-gates-real-llm.md) | Darwin Mode — the real surface gates a real LLM on a real test (ADR-098 nucleus) | Accepted (measured PoC) — the ADR-098 nucleus | ADR-106 ran real surface code on synthetic tasks; ADR-107 had a real LLM fix a real test (but the surfaces weren't in the loop). This ADR joins them: a variant's real contextBuilder decides which files a real LLM may see, and a real test is the verdict. It demonstrates the core self-improvement prem |
| [ADR-110](./ADR-110-darwin-real-llm-evolution-capstone.md) | Darwin Mode — evolution improves a real LLM's real-test pass-rate (capstone) | Accepted (measured) — the capstone | ADR-109 showed a *static* contrast: a wide-window variant lets a real LLM fix a real test that a narrow one cannot. This ADR closes the loop dynamically: a real evolutionary search, scored end-to-end by the real surface→real-LLM→real-test pipeline, *discovers* the better harness and lifts a real LLM |
| [ADR-111](./ADR-111-darwin-falsification-window-not-ranking.md) | Darwin Mode — falsification: it's the context WINDOW SIZE, not the ranking (self-correction) | Accepted (falsification — corrects ADR-109/110 overclaims) | An adversarial review flagged that ADR-109/110 may show only a context-WINDOW-SIZE effect, not contextBuilder RANKING quality. I ran the proposed null-model falsification. The review was right. This ADR records the result and corrects the earlier framing — testing and falsifying our own claims is th |
| [ADR-112](./ADR-112-darwin-fdr-calibration-small-n.md) | Darwin Mode — FDR gate is uncalibrated at small n (self-correction of ADR-096) | Accepted (falsification — corrects/conditions ADR-096) | The adversarial review (A2) noted ADR-096 validated BH only on idealised `Uniform(0,1)` nulls, not on the bootstrap p-values at the small sample sizes `evolve` actually uses. I ran the calibration check. The gate does not control FDR at n=3. This conditions the ADR-096 claim and is recorded as a rea |
| [ADR-113](./ADR-113-darwin-ranking-matters-varied-relevance.md) | Darwin Mode — contextBuilder ranking IS causal when relevance varies (completes ADR-111) | Accepted (measured) — completes the ADR-111 falsification into the full picture | ADR-111 honestly showed the contextBuilder's *ranking* contributed nothing — but only because its distractors shared the buggy file's terms (flat overlap → ranking degenerates to input order). That left an open question the review implied: does ranking matter when file relevance actually *varies*? I |
| [ADR-114](./ADR-114-darwin-diversity-advantage-substrate-dependent.md) | Darwin Mode — the diversity-beats-greedy result is substrate-dependent (does NOT replicate on the real-surface substrate) | Accepted (measured) — a non-replication that tempers ADR-105 | The review's highest-value next step was: replicate ADR-105's "diversity beats greedy on deception" on the *real* substrate before committing SWE-bench tokens — "if it does not replicate, you learn something important." It did not replicate. That is the finding, and it is more valuable than a confir |
| [ADR-115](./ADR-115-darwin-crossover-ablation-archive-retention.md) | Darwin Mode — crossover ablation refutes "crossover is load-bearing"; the archive does the work (corrects ADR-114) | Accepted (measured) — refutes a hypothesis from ADR-114 | ADR-114 concluded "crossover is the load-bearing mechanism, not the selection strategy." That was a hypothesis from runs that all had crossover ON. The clean test is an ablation. It refutes the claim — and reveals the actual mechanism is the retained archive. |
| [ADR-116](./ADR-116-darwin-retention-ablation-inconclusive.md) | Darwin Mode — retention ablation (partial support, inconclusive; thread wound down) | Accepted (measured — partial/inconclusive, honestly) | ADR-115 claimed whole-archive retention enables sequential two-surface accumulation, making crossover optional — and predicted that *without* retention, crossover becomes necessary. This ablation tests both halves. The first is partially supported; the second is not. Recorded honestly, including the |
| [ADR-117](./ADR-117-darwin-swe-nucleus-real-multifile.md) | Darwin Mode — real multi-file SWE nucleus (surface-selected, LLM-reasoned, real test) | Accepted (measured PoC) — the ADR-098 nucleus with real content | ADR-109 proved the surface gates a real LLM's *access*, but used a hardcoded fix. The genuine SWE step is the LLM *reasoning over real multi-file code* — choosing which file is buggy and fixing it — with a real test as the only verdict. This is that, at micro scale. |
| [ADR-118](./ADR-118-darwin-swe-suite-generalizes.md) | Darwin Mode — the SWE-nucleus loop generalizes across varied real bugs | Accepted (measured) — generalization of ADR-117 | ADR-117 fixed one real multi-file bug end-to-end. The obvious skeptic's question: a fluke? This runs five independent bugs across different domains through the same real loop. 5/5. |
| [ADR-119](./ADR-119-darwin-multidomain-evolution-capstone.md) | Darwin Mode — evolution lifts real-LLM real-test pass-rate across a multi-domain suite | Accepted (measured) — multi-domain dynamic capstone | ADR-110 evolved one harness parameter to lift a single real-LLM task; ADR-118 showed the real loop generalizes statically across five domains. This ties them: a real evolutionary loop that lifts the real-LLM real-test pass-rate across all five domains at once. |
| [ADR-120](./ADR-120-darwin-swe-loop-on-real-package-code.md) | Darwin Mode — the SWE loop fixes a bug in THIS package's real production code | Accepted (measured) — closes the "toy repos" caveat of ADR-117/118 | ADR-117/118 used hand-built toy repos. The skeptic's caveat: real code is bigger and messier. This runs the loop on this package's own production TypeScript — the contextBuilder selects among the 21 real `src/*.ts` files, and a real LLM fixes a real bug in real code, verified by a real test. |

### Darwin Mode applied to SWE-bench code repair (ADR-121…155)

| ADR | Title | Status | Summary |
|---|---|---|---|
| [ADR-121](./ADR-121-darwin-swe-loop-real-vitest-oracle.md) | Darwin Mode — the SWE loop verified by the package's OWN committed vitest suite | Accepted (measured) — most authentic real-code oracle in the series | ADR-120 fixed a bug in the package's real `pareto.ts`, but the verdict was a *hand-written* contract assert. The skeptic's remaining gap: "your oracle isn't the real test suite." This closes it — the verdict is the package's own committed `pareto.test.ts`, run under vitest. |
| [ADR-122](./ADR-122-darwin-longhorizon-validation-harness.md) | Darwin Mode — the long-horizon Validation Harness (ADR-098 step 1) | Accepted (measured) — implements ADR-098 step 1, the "de-risk before the real benchmark" milestone | ADR-098 step 1 (the recommended starting point, before any external dataset/budget): "Build a synthetic ~50-file repository stress-test that exercises context management over 50+ sequential steps — the regime where agents *lose the thread*. Verify Darwin's loop holds state before exposing it to a re |
| [ADR-123](./ADR-123-darwin-swe-bench-adapter.md) | Darwin Mode — the SWE-bench BenchmarkRunner adapter + real resolved criterion (ADR-098 step 2) | Accepted (measured) — implements ADR-098 step 2 | ADR-098 step 2: "Conform the harness to a standard runner contract (SWE-bench Verified task format) so results are apples-to-apples." This ships the adapter — and, crucially, the real SWE-bench "resolved" criterion that ADR-117…121 did not check: a patch counts only if every `FAIL_TO_PASS` test goes |
| [ADR-124](./ADR-124-darwin-gitapply-patch-primitive.md) | Darwin Mode — the `git apply` patch primitive, and why whole-file beats raw LLM diffs (ADR-098 step-3 prep) | Accepted (measured) — last autonomous step-3 prep; informs the step-3 patch primitive | ADR-123 applied patches by whole-file replacement. Real SWE-bench applies a unified diff with `git apply` (model output is a diff). This validates a real `git apply` primitive end-to-end — and surfaces a concrete finding about which patch representation is reliable. |
| [ADR-125](./ADR-125-darwin-consolidated-swe-runner.md) | Darwin Mode — the consolidated `runSweBenchTask()` corpus-ready runner | Accepted (measured) — the single entry point an external corpus iterates | ADR-122/123/124 proved the pieces separately (validation harness, resolved criterion, patch primitive). This consolidates them into ONE function — `runSweBenchTask(task, opts)` — so step 3 is literally `for (const task of dataset) await runSweBenchTask(task, { model, key })`. |
| [ADR-126](./ADR-126-darwin-repair-loop-and-robust-parsing.md) | Darwin Mode — iterative repair loop, regression-aware feedback, and robust patch parsing | Accepted (measured) — runner capability upgrade + an honest limitation finding | ADR-125's runner did one shot per instance. This adds an iterative repair loop (feed failing tests back, retry up to N), regression-aware feedback (also report newly-broken `PASS_TO_PASS`), and robust patch parsing. The robust parsing fixes a real latent bug; the repair loop helps on single-fault in |
| [ADR-127](./ADR-127-darwin-searchreplace-patch-primitive.md) | Darwin Mode — the search/replace patch primitive (fixes ADR-126's large-file limitation) | Accepted (measured) — new default patch primitive; resolves the ADR-126 finding | ADR-126 found whole-file repair reliable on small files but regressing `PASS_TO_PASS` on large ones (a full rewrite changes more than intended). ADR-124 found raw LLM unified diffs corrupt. The middle path — used by real coding agents (Aider) — is a search/replace edit: an exact `old → new` block. T |
| [ADR-128](./ADR-128-darwin-contextbuilder-camelcase.md) | Darwin Mode — contextBuilder camelCase tokenization (and the limit of path-based selection) | Accepted (measured) — core harness fix for the ADR-127 file-selection finding | ADR-127 found the contextBuilder mis-selects files when a bug report names camelCase symbols: `paretoFront` lowercased+split to `paretofront`, which never matched the file `pareto.ts`. This fixes the tokenizer — and measures exactly how far a path-based fix can go. |
| [ADR-129](./ADR-129-darwin-symbol-index-selection.md) | Darwin Mode — symbol-index file selection (closes the ADR-127/128 selection gap) | Accepted (measured) — completes the file-selection finding | ADR-128 fixed selection when a symbol's stem matches its filename (`paretoFront`→`pareto.ts`) but showed path tokenization cannot find a symbol whose name differs from the file (`poincareDistance` ∈ `phenotype.ts`). This adds symbol indexing and closes that gap. |
| [ADR-130](./ADR-130-darwin-swe-fitness-function.md) | Darwin Mode — `runSweBenchTask` as a fitness function for harness selection | Accepted (measured) — closes the loop between the SWE runner and the evolutionary engine | The whole SWE runner (ADR-123–129) exists so the harness can be *scored on real tasks*. This closes the loop: a config population is evaluated by the real resolved-criterion over a small corpus, and the SWE resolve-rate (tie-break: cost) selects the best harness configuration — the exact signal `evo |
| [ADR-131](./ADR-131-darwin-swe-runner-external-package.md) | Darwin Mode — the SWE runner generalizes to an external package | Accepted (measured) — first autonomous step into the ADR-098 "external" gate | ADR-120–130 validated the SWE runner on `darwin-mode` — the package it was built in. The open question for ADR-098 was whether it works on code it was *not* built around. This runs it on a different package in the monorepo (`kernel-js`) with its own conventions, files, and vitest suite — the first c |
| [ADR-132](./ADR-132-darwin-monorepo-corpus.md) | Darwin Mode — a multi-package self-hosted SWE corpus (cross-package resolve-rate) | Accepted (measured) — ADR-098-step-3-flavored result at monorepo scale | ADR-131 showed the runner resolves a bug in one external package. This scales to a real cross-package resolve-rate: one known bug in each of four monorepo packages — different codebases, conventions, and vitest suites — all scored by the *same* runner under the real resolved-criterion. The honest mi |
| [ADR-133](./ADR-133-darwin-evolve-against-swe-fitness.md) | Darwin Mode — evolving the harness against a real cross-package SWE fitness (capstone) | Accepted (measured) — the "evolve it, optimize" capstone; unites all four pillars | The whole session built toward this: an actual evolutionary loop optimizing the harness against a real SWE objective on real external code. It unites the four pillars — evolutionary engine + SWE runner + multi-package corpus + fitness function. |
| [ADR-134](./ADR-134-darwin-capability-driven-evolution.md) | Darwin Mode — capability-driven evolution on a discriminating corpus (completes ADR-133) | Accepted (measured) — the capability half of the evolve capstone | ADR-133 evolved over an easy corpus where resolve-rate saturated, so evolution converged on cost. The honest question it raised: does capability drive evolution when the corpus is hard enough to discriminate? This answers it with a corpus that includes a real multi-fault bug (a legitimate, common ca |
| [ADR-135](./ADR-135-darwin-swe-fix-model-frontier.md) | Darwin Mode — the SWE-fix model frontier (the default model is suboptimal) | Accepted (measured) — a real "optimize" finding on the model axis | The runner (123–134) has only ever used `gemini-2.5-flash`. "Optimize" raises the obvious question the SWE arc never asked: *which model* maximizes resolve-rate per dollar on the SWE-fix task? This is ADR-085's cheap-beats-frontier check applied to `runSweBenchTask` — and it finds the current defaul |
| [ADR-136](./ADR-136-darwin-fullgenome-local-optimum.md) | Darwin Mode — full-genome evolution hits a local optimum (why diversity/crossover exist) | Accepted (measured) — an honest negative result that re-motivates the engine's machinery | ADR-135 found `deepseek/searchreplace` is the cheapest full-resolve config by a *manual* frontier sweep. This asked the obvious follow-up: can the `(1+λ)` evolve loop discover it autonomously by mutating a `model` gene alongside `{patchMode, maxAttempts}`? The honest answer is no — naive single-gene |
| [ADR-137](./ADR-137-darwin-evolve-noise-floor-and-epistasis.md) | Darwin Mode — the micro-evolve noise floor + model×patchMode epistasis (honest stop) | Accepted (measured) — an honest limitation finding; concludes the micro-evolve thread | ADR-137 set out to show diversity+crossover escaping ADR-136's local optimum by recombining the `deepseek` and `searchreplace` genes (the ADR-105 analogue on real SWE code). It did not — and the reasons are themselves the finding: per-cell LLM-fitness variance dominates at this scale, and the genome |
| [ADR-138](./ADR-138-darwin-fitness-noise-quantified.md) | Darwin Mode — the micro-evolve fitness noise floor, quantified | Accepted (measured) — converts ADR-137's qualitative claim into a number | ADR-137 *asserted* that per-cell LLM fitness variance dominates single-run micro-evolve. This measures it: each genome run repeatedly on the same 3-package corpus, reporting the resolve-count distribution — the rigorous basis for "average N runs" and for the ADR-137 stop. |
| [ADR-139](./ADR-139-darwin-default-validated.md) | Darwin Mode — the deepseek default, validated under averaging (and it's also the most stable) | Accepted (measured) — averaged validation of the ADR-135 default change | ADR-135 chose deepseek-chat as the SWE-fix default from a *single* run, and that default was shipped (commit 2bd4cee). ADR-138 then showed single runs are noisy. The responsible follow-up — applying the noise-floor lesson to the decision itself — is to validate the change with averaged runs before r |
| [ADR-140](./ADR-140-darwin-mapelites-crossover-capstone.md) | Darwin Mode — diversity + crossover + averaging assembles the optimum naive search missed (rigorous capstone) | Accepted (measured) — the rigorous close to the evolve arc; ADR-105 reproduced on real SWE code | ADR-136 (naive n=1 hill-climb) and ADR-137 (naive crossover) both failed to reach `deepseek/searchreplace`. This combines the three fixes those failures motivated — diversity-preserving selection (per-model MAP-Elites niches), crossover across niche elites, and averaged fitness (n=3, ADR-138) — and  |
| [ADR-141](./ADR-141-darwin-capability-discriminating-capstone.md) | Darwin Mode — the full machinery reaches the global optimum on a discriminating corpus (definitive capstone) | Accepted (measured) — removes ADR-140's caveat; the definitive close to the evolve arc | ADR-140 showed diversity+crossover+averaging *assembles* `deepseek/searchreplace`, but on an easy corpus resolve-rate saturated (4/6 at ceiling) so cost broke a wide tie — leaving the caveat "resolve-rate didn't discriminate." This re-runs on a capability-discriminating corpus where resolve-rate its |
| [ADR-142](./ADR-142-swebench-lite-pilot.md) | Darwin Mode — first real canonical SWE-bench number (Lite pilot, ADR-098 boundary lifted) | Accepted (measured) — the first real external-benchmark run; lifts the ADR-098 honest boundary | Since ADR-098 the project refused to claim an external number until a real run existed. This is that run: the validated Darwin harness, on a stratified sample of canonical SWE-bench Lite (Python), scored by the official `swebench` Docker harness. The honest boundary is now lifted — replaced by a rea |
| [ADR-143](./ADR-143-swebench-repair-loop-ab.md) | Darwin Mode — closed-loop repair vs open-loop on real SWE-bench (controlled A/B) | Accepted (measured) — Stage B2; honest A/B, mechanism confirmed, headline within noise at n=25 | ADR-142 set the open-loop floor (3/25 = 12%). This turns ADR-126's repair loop back on against the same stratified 25 SWE-bench Lite instances — a controlled A/B: open-loop single-shot vs closed-loop (run the real tests in Docker, feed the traceback back, retry up to 3×). |
| [ADR-144](./ADR-144-swebench-lite-full-300-baseline.md) | Darwin Mode — full SWE-bench Lite baseline (all 300, open-loop, fixed deepseek) | Accepted (measured) — the definitive, un-cherry-picked, tight-CI Darwin SWE-bench Lite number | ADR-142/143 measured cherry-picked stratified-25 samples (12–16%). This is the whole benchmark: all 300 SWE-bench Lite (test) instances, open-loop fixed-deepseek, scored by the official `swebench` Docker harness. No selection bias, n=300, a genuinely tight CI — the honest headline figure. |
| [ADR-145](./ADR-145-metaharness-router-swebench-integration.md) | Integrate `@metaharness/router` into the SWE-bench solver + add `model` as an evolvable surface | Proposed (roadmap — not yet implemented; deferred behind the ADR-144 full-300 baseline) | The user's observation: the metaharness already ships a learned cost-optimal router (`packages/router`, `@metaharness/router`), and the SWE-bench solver currently fixes one model (`deepseek-chat`) for every instance. Routing each instance to the *cheapest model good enough for it* — and making that  |
| [ADR-146](./ADR-146-swebench-localization-emission-wall.md) | Darwin Mode — LLM file-localization lifts recall +15pp but exposes the "emission wall" | Accepted (measured) — Stage-B context fix; recall win, resolve-rate flat, bottleneck relocated | ADR-144's baseline had a 67% empty-patch rate. The diagnostic (this ADR's first half) showed selection recall was only 44.7% and 65% of empties were selection-misses → localization was the indicated fix. This ADR ships it, measures it on the full 300, and reports an honest result: recall rose +15pp, |
| [ADR-147](./ADR-147-metaharness-darwin-deep-integration.md) | Deep-integrate Darwin Mode into the `metaharness` scaffolder | Accepted (implemented, tested, published) — `metaharness@0 | Previously `metaharness` (the `npx metaharness` scaffolder) and `@metaharness/darwin` (the evolutionary engine) were sibling packages with no integration — generated harnesses shipped only a stub `evolve` skill. This makes the integration real: every scaffolded harness gets working Darwin Mode self- |
| [ADR-148](./ADR-148-barbarian-scholar-hybrid-escalation.md) | Barbarian & Scholar — cheap→frontier hybrid escalation | Accepted (measured) | 1. Barbarian (cheap, deepseek-V3 + repair) sweeps all 300 and banks 46 easy wins at ~$0.01/instance. |
| [ADR-148](./ADR-148-swebench-hybrid-escalation-router.md) | SWE-bench hybrid cheap→frontier escalation ("Barbarian and the Scholar") | Proposed (design; measurable — gated on the ADR-149 repair-300 hard-tail) | The cheap model (`deepseek-chat`) has a reasoning ceiling: even with the exact pytest traceback, some bugs exceed its capacity to synthesize correct logic. Hardwiring a frontier model everywhere is economically absurd ($2–8/instance × 300 ≈ $600–2400/run, destroying the evolutionary premise). This A |
| [ADR-149](./ADR-149-repair-loop-full-scale-swebench.md) | Closed-loop repair at full scale — SWE-bench Lite 300 | Accepted (measured) | essentially disjoint (baseline upper 11.2 vs repair lower 11.7) — a statistically meaningful lift, |
| [ADR-150](./ADR-150-tailscale-local-frontier-concurrent-benchmarks.md) | Tailscale-served local frontier model + concurrent benchmark tracks | Proposed (architecture + harness support implemented; live run gated on the Mac being online) | A 48 GB-unified-memory Mac (Studio/mini) on the tailnet is a private, $0-inference "frontier-tier" OpenAI-compatible endpoint. A `Qwen2.5-Coder-32B` GGUF (Q4/Q5, ~19–23 GB) fits alongside Docker (~12 GB) under 48 GB with metal acceleration. Over Tailscale it looks exactly like the OpenAI API to the  |
| [ADR-151](./ADR-151-v4pro-cheap-floor.md) | deepseek-v4-pro lifts the cheap Barbarian floor 15.3% → 29.3% | Accepted (measured) | harness (localize + search/replace + ≤3 test-feedback repair), no frontier escalation. 209/300 |
| [ADR-152](./ADR-152-v4pro-scholar-hybrid.md) | v4-pro + Scholar hybrid — new ceiling 40.3% on SWE-bench Lite | Accepted (measured) | Barbarian + 33 from the sonnet-4 Scholar on the 212-instance tail v4-pro couldn't crack (33/212 = |
| [ADR-153](./ADR-153-agentic-loop-architecture-proposal.md) | Beyond escalation — the agentic-loop architecture for the 65–88% tier | Implemented + measured at scale — 275/300 = 31 | The single-shot-localize + search/replace + repair + tiered-escalation paradigm has been pushed to a |
| [ADR-154](./ADR-154-three-tier-58pct.md) | Three-tier cheap→frontier→frontier hybrid — 58.3% on SWE-bench Lite (verified) | Accepted (measured + reproducibility-verified) | 212-tail (+33 → 121) → opus-4.8 Sage on the residual (+54 → 175). |
| [ADR-155](./ADR-155-darwin-shield-defensive-zero-day-harness.md) | Darwin Shield — evolving a defensive zero-day discovery harness | Proposed (reference implementation landed — see §Reference implementation) | This is the security application of the Darwin Plus stack (ADR-077…081). It changes the *task* — defensive vulnerability discovery instead of SWE-bench repair — and the *fitness function*, but keeps the load-bearing thesis intact: the foundation model stays frozen; the harness evolves; the proof is  |

### poker-darwin, SOTA leaderboard integrity, and later Darwin experiments (ADR-169…239)

| ADR | Title | Status | Summary |
|---|---|---|---|
| [ADR-169](./ADR-169-self-learning-wasm-memory-escalator.md) | Self-learning WASM-memory cost-optimal escalator (E2/E3/E4) | Implemented — cores + tests ($0, offline); empirical at-scale runs deferred (budget) | (tiering), ADR-153 (agentic loop), ADR-154 (58.3% 3-tier). Incorporates an external peer review. |
| [ADR-170](./ADR-170-stateful-pty-agent.md) | Transition from `searchreplace` to a stateful PTY agent loop | Proposed (blueprint; implementation + batch-validation are the next arc) | Numbering note. Authored as "ADR-151" in the proposal, but ADR-151 is already in use |
| [ADR-171](./ADR-171-webui-darwin-model-config.md) | Web-UI — surface Darwin capabilities + model-tier configuration | Accepted — implementing in `apps/web-ui` | (agentic loop, self-learning, PTY), the live Studio at https://ruvnet.github.io/agent-harness-generator |
| [ADR-172](./ADR-172-road-to-sota.md) | Road to SOTA — pushing SWE-bench Lite past 70% | Proposed — roadmap; levers prioritized by measured ROI | router / anti-thrash), ADR-170 (stateful-PTY loop), ADR-171 (web-UI). Grounded in RESULTS §22–29. |
| [ADR-173](./ADR-173-leaderboard-conformant-top10.md) | Leaderboard-conformant top-10 on SWE-bench Lite → Verified, at the cost-per-resolve frontier | Accepted — implementing (conformant solver first) | escalation-cascade approach *for leaderboard purposes* (that path uses an in-loop oracle — see below). |
| [ADR-174](./ADR-174-road-to-number-one.md) | Road to #1 overall — DeepSeek-V4-Flash base + Test-Critic + Conformant MCTS + Opus-Sniper | Implementing — L0 | showed a low submit-rate — a linear agentic loop hits a context-collapse wall and won't reach |
| [ADR-175](./ADR-175-dual-mode-test-driven-repair.md) | Dual-mode repair — Test-Driven Repair (oracle-ON) vs Conformant autonomous repair (oracle-OFF) | Accepted | distinction surfaced in LEARNINGS §9. |
| [ADR-176](./ADR-176-swe-conductor.md) | SWE Conductor — role-specialized repair with deterministic verifiers + asymmetric escalation | Proposed (empirically grounded by ADR-174 measurements) | Three conformant batches (25-instance Lite pilots, gold-graded) isolated the bottleneck precisely: |
| [ADR-177](./ADR-177-conformant-ceiling-synthesis.md) | The conformant ceiling — ablation synthesis + honest path (SOTA push conclusion) | Accepted (architecture conclusion; cheap-lever search exhausted) | A LEGITIMATE, leaderboard-conformant top-10 on SWE-bench Lite (≥45%) at the cost-per-resolve frontier |
| [ADR-178](./ADR-178-bestofn-llm-judge-discriminator.md) | — Best-of-N selection via env-filter + LLM-judge discriminator | Proposed | The stateful interactive ReAct loop (conformant, repo's-own-tests as the regression gate) broke the MCTS |
| [ADR-179](./ADR-179-cost-pareto-leaderboard.md) | — Cost–Performance Pareto leaderboard (Value Score, multi-benchmark, public) | Proposed | Public SWE-bench leaderboards rank by raw resolve % only, hiding cost. Darwin's whole thesis is |
| [ADR-180](./ADR-180-distributed-benchmark-runs-gcp-firestore.md) | — Distributed benchmark runs: GCP VM runner + Firestore results store | Proposed | Getting Darwin's *own* numbers on SWE-bench Verified (500) and Pro (731) needs their Docker images |
| [ADR-181](./ADR-181-multimodel-multibench-cost-pareto-fleet.md) | — Multi-model, multi-benchmark cost-Pareto fleet | Proposed | The cost-Pareto thesis is strongest when shown across *many cheap models* and *many benchmarks*, not one. |
| [ADR-182](./ADR-182-cost-cascade-conditional-bestofn.md) | — Cost cascade (conditional Best-of-N) via the repo-test gate | Proposed | Parallel Best-of-N pays for all N trajectories on every instance regardless of difficulty (39.7% @ $0.015, |
| [ADR-183](./ADR-183-judge-validated-repro-gate.md) | — Judge-validated self-written repro test as a resolution gate (Goodhart counter-measure) | Proposed | ADR-182 (cascade), §18 (Best-of-3 = 39.7% @ $0.015 — the bar) |
| [ADR-184](./ADR-184-sovereign-evolution-genome-engine.md) | — Sovereign Evolution: evolve the solver architecture (genome engine) | Proposed | We mapped the SWE-solver architecture space BY HAND (single → Best-of-N → discriminator → cascade → repro-gate). |
| [ADR-185](./ADR-185-sota-breaking-levers-research.md) | — SOTA-breaking levers beyond the empty-patch cascade (research → roadmap) | Proposed | Darwin's conformant cost-Pareto frontier stands at GLM→Opus empty-patch cascade = 51.3% @ $0.267/inst (n=300), |
| [ADR-189](./ADR-189-chebyshev-temperature-schedule.md) | — Chebyshev-Scheduled Temperature Curves for ReAct Trajectory Stabilization | Proposed | Phase-2 generalization (queued): replace the fixed raised-cosine with a full Clenshaw-evaluated Chebyshev polynomial whose coefficient array is *evolved* by Darwin (as in ADR-188), so the curve's shape is tuned to measured resolve rather than hand-set. The current direct form is the clean single-var |
| [ADR-190](./ADR-190-ast-fused-mincut-localization.md) | — AST-Fused Dynamic Mincut for Structural Pre-Seeding (localization) | Proposed | --- |
| [ADR-191](./ADR-191-timesfm-ruvector-translation.md) | — TimesFM → `ruvector` translation (candle) via chunked iterative synthesis | Proposed | --- |
| [ADR-192](./ADR-192-polyglot-solver-pro-eval.md) | — Polyglot solver + SWE-bench Pro eval hardening | Proposed | --- |
| [ADR-193](./ADR-193-reproduction-test-selection.md) | Reproduction-test SELECTION — a conformant, Goodhart-free Test-Driven-Repair analog | Accepted (built + conformant) · **Result: NEGATIVE-leaning** (selection ≈ judge; the +1 is noise/artifact, not the repro signal) | Test-Driven Repair (write a test, then patch until it passes) is a top-leaderboard pattern, but the naive form is non-conformant: if the solver optimizes its patch against its *own* model-written test, it self-grades and Goodharts (ADR-174/§10 — a weak model authors a weak repro, then games it). Thi |
| [ADR-194](./ADR-194-crack-the-tail-per-instance-evolution.md) | — Crack-the-tail: per-instance parallel evolution → generalizable capabilities → conformant validation | Proposed | --- |
| [ADR-195](./ADR-195-phase2-capability-stack.md) | — Phase-2 capability stack: localization, reproduction-gate, reviewer (the new-code harness investments) | Proposed | --- |
| [ADR-196](./ADR-196-execution-trace-localization.md) | — Execution-trace localization: the dynamic localizer (repro → run → trace → fix-site) | Proposed | --- |
| [ADR-197](./ADR-197-redblue-adversarial-harness.md) | MetaHarness Adversarial Operators (`@metaharness/redblue`) — a safety-gated Red/Blue team harness | Accepted (shipped) | The MetaHarness thesis is "freeze the model, evolve the harness." This ADR applies the same controlled-harness discipline to security testing: use uncensored OpenRouter models as adversarial *system actors* (simulated insiders/attackers/careless operators) against a target you own, while the harness |
| [ADR-198](./ADR-198-weight-eft-hybrid-policy-weight-evolution.md) | Weight-EFT (`@metaharness/weight-eft`) — hybrid policy + weight evolution | Accepted (scaffold shipped; training GPU-gated) | MetaHarness's thesis is "freeze the model, evolve the harness" — gradient-FREE policy evolution. This ADR adds the *complementary* lever: gradient/weight self-learning on the open cheap tier. We distil the harness's archival success into GLM/Qwen via LoRA so the cost-cascade escalates to a frontier  |
| [ADR-199](./ADR-199-cve-fix-security-benchmark.md) | CVE-fix Security Benchmark ("SWE-bench-Security" track) | Accepted (seed shipped) | The Darwin thesis is "freeze the model, evolve the harness," measured on real external |
| [ADR-201](./ADR-201-vector-memory-graphrag-cheap-model-lift.md) | Vector-Memory Ablation — does GraphRAG (ruvector) lift cheap models over the turn-budget cliff? | Proposed | The cheap-vs-frontier campaign proved that on everyday-agentic work (FRAMES QA, BFCL tool-use) cheap models (DeepSeek-V4-Pro, GLM-5.2) are at parity-or-better with older-frontier at 2–56× lower cost, but the gap persists on hard code (SWE-Pro 4% turn-budget cliff). The hypothesis under test here: ve |
| [ADR-202](./ADR-202-agenticow-jujutsu-dual-state-bridge.md) | Dual-state branching — agentic-jujutsu (code/op) ⇄ agenticow (memory) | Proposed | A coding agent that explores must branch and roll back two state planes: |
| [ADR-203](./ADR-203-cognitum-fugu-completions-api.md) | — Cognitum Fugu: a GCP-hosted, metered, tiered Completions API on MetaHarness | Proposed | `ruvnet/midstream` for inflight streaming escalation (new §3.5 + a §10 risk row + |
| [ADR-204](./ADR-204-unlimited-subscription-tiered-economics.md) | — "Unlimited"-feeling flat-rate subscriptions on Cognitum Fugu: business model + budget-defense architecture | Proposed | Fair-use unlimited. Everyday work is effectively uncapped because it runs on the cheap |
| [ADR-205](./ADR-205-harness-handoff-beats-model-embedding.md) | — Harness handoff beats model embedding: darwin as router, claude -p as hard-tail actuator | Accepted (benchmarked) | - Status: Accepted (benchmarked) |
| [ADR-206](./ADR-206-benchpress-low-rank-score-prediction.md) | BenchPress-style low-rank score prediction — model onboarding & tier placement for MetaHarness | Proposed | - Status: Proposed |
| [ADR-228](./ADR-228-gepa-distilled-executor-genome.md) | GEPA-distilled executor genome — offline evolutionary optimization of the cheap executor's operating policy | Proposed | Numbering note. This ADR continues the cross-repo 2xx decision thread established in ADR-226 §"Numbering note": ADR-207–225 live in the meta-llm repo (`cognitum-one/meta-llm`, `docs/adr/`), ADR-226 lives here, and ADR-227 is reserved cross-repo for meta-llm's completions-gateway data flywheel (the t |
| [ADR-229](./ADR-229-rli-economic-workflow-oracle.md) | RLI (Remote Labor Index) as the economic-workflow-performance oracle — internal target generation (primary) + tier-placement calibration (secondary) | Proposed | This is the headline. RLI's primary value to us is generative — it produces the internal north-star targets and verifier standard for the vertical loop. Everything below (tier placement) is a secondary application of the same oracle. See the harnessaas RLI-Mini ADR for the pipeline; this ADR establi |
| [ADR-237](./ADR-237-evals-math-live-gsm8k-fast-domain.md) | evals-math LIVE GSM8K — a FAST, $0 real-compounding domain testbed | Accepted — live run COMPLETE | - Status: Accepted — live run COMPLETE. Mechanism proven end-to-end on a SECOND real domain (GSM8K) at $0; compounding is an HONEST NULL (0/16 promotions), with the gate correctly rejecting a harmful mutation. Confirms the recover-not-create ceiling (ADR-234) is domain-general, not a SWE-bench artif |
| [ADR-238](./ADR-238-workspace-lens-jacobian-interpretability.md) | @metaharness/workspace-lens — Jacobian-Lens interpretability primitive (IntOps) | Accepted — runtime measurement core shipped ($0, synthetic-tested) | - Status: Accepted — runtime measurement core shipped ($0, synthetic-tested). Lens FITTING is external (open-weight model + backward pass); this package APPLIES a fitted lens. |
| [ADR-239](./ADR-239-workspace-probe.md) | @metaharness/workspace-probe — evaluation + Darwin-Mode bridge for workspace-lens | Accepted — logic developed + validated in isolation against the published `@metaharness/workspace-lens@0 | - Status: Accepted — logic developed + validated in isolation against the published `@metaharness/workspace-lens@0.1.0` ($0, 9 tests); re-homed into the monorepo with a local workspace dep. |
| [ADR-251](./ADR-251-governed-autonomous-variation-runtime.md) | `@metaharness/avo` — governed autonomous variation runtime | Accepted — runtime + deterministic RVF restart proof implemented; live 100-task unseen SWE-bench ship gate pending | Stateful evidence-driven variation, real tool receipts, semantic supervision, structured RVF memory, immutable authority, and fail-closed claims. |
| [ADR-252](./ADR-252-latentmesh-latent-state-mesh.md) | LatentMesh — causally-verified latent-state communication between agents | Proposed — external Rust prototype adopted as planned integration; no code in this repo yet | Latent-frame transfer under evidence-before-authority gates: five-control causal verification, edge authority mapped onto ADR-251 capability policy, topology evolution via Darwin, RVF-replayable audit. |
| [ADR-253](./ADR-253-arc-agi-3-chatgpt-harness.md) | Experimental ARC-AGI-3 controller and ChatGPT Developer Mode adapter | Implemented experimentally — deterministic mechanism tests required; official public scorecard pending | Provider-neutral exact-frame controller plus a remote MCP and MCP Apps adapter that uses ChatGPT as the OpenAI reasoning host without an OpenAI API key. Separate from ADR-251 and ineligible for an AVO-class or 100% claim until the frozen official gate passes. |
| [ADR-271](./ADR-271-avo-receipts-as-flywheel-gate-evidence.md) | AVO receipts as flywheel-gate evidence: reconciling harness and gateway governance | **Implemented (harness-side)** | Answers "is meta-llm updated with AVO capabilities?" — no, and it shouldn't be by porting AVO's runtime, because meta-llm already implements the same governance model (OaK/flywheel-gate/genome). SHIPPED `@metaharness/avo` `flywheelGate.ts` — `receiptsToGateInput`/`submitToFlywheelGate`/`gateTrustedKey` serialize an AVO run's signed receipts to the gate's five `SignedReceipt`s and submit to `POST /v1/flywheel/gate`; the gate's `meetsPromotionRule` + verify checks govern promotion unchanged. Corrected for meta-llm #118 (signer allowlist). AVO 10/10 + meta-llm contract 7/7 green. Non-goals: no `GovernedVariationOperator` runtime in the gateway, no auto-serve. Mirrored to meta-llm PR #356. |
| [ADR-254](./ADR-254-arc-avo-controlled-ablation.md) | ARC-specific AVO loop and controlled ablation | Implemented experimentally — synthetic gate passed; one actor-declared-clean single-game smoke favored AVO; full official gate pending | Enforced candidate selection, lineage, memory, blocking supervision, and a separately reported retrodiction extension, with compute-matched controls and a preregistered official RHAE claim gate. |
| [ADR-274](./ADR-274-flywheel-replay-consolidation-and-chain-binding.md) | Consolidating three unmerged flywheel-replay trust-boundary fixes, and closing a 4th (chain-shape binding) | Accepted | Hand-merges 3 conflicting-but-compatible unmerged Dream Cycle PRs (#205, #231, #255) into one clean diff on `verifyReplayBundle`/`gateReExecutes`/`sealedFieldsAuthentic`, and closes a 4th gap an independent critic found in review: `parents`/`generation` were unbound, letting two genuinely-signed commits be spliced into a fabricated chain via unsigned fields. 72/72 tests, all 7 real committed ReplayBundles re-verify unchanged. Written to reduce the Dream Cycle PR review backlog, not add to it. Renumbered twice on 2026-09-01: ADR-272 → ADR-273 after #260 independently claimed ADR-272, then ADR-273 → ADR-274 after PR #179's merge (`c441174`) independently claimed ADR-273 for the unrelated "Dream Machine" scheduler ADR. |
| [ADR-275](./ADR-275-evidence-carrying-termination.md) | Evidence-carrying termination — gate the `COMPLETE` boundary on a typed certificate binding required claims to in-scope trace evidence, with deterministic replay | Proposed | Renumbered from ADR-272 (taken by the darwin numeric genome kind, #260) — see #267. |
| [ADR-276](./ADR-276-avo-release-claim-evidence-gate.md) | AVO exact-SHA release claim evidence gate | Accepted — verifier, governed-surface scan, CI, and mandatory publish gate implemented; restricted evidence pending | Blocks performance/frontier publication unless exact protected claim semantics bind to a preregistered unseen 100-task benchmark, exact tag SHA, measured costs, lineage roots, trusted signatures, and two independent official graders. |
## Autogenous integration

| ADR | Title | Status | Summary |
|-----|-------|--------|---------|
| [ADR-277](./ADR-277-autogenous-metaharness-adapter.md) | Autogenous governed MetaHarness adapter | Accepted (implemented) | Ships `@metaharness/autogenous`: a flywheel adapter for Autogenous radio-moe that exposes only five bounded policy levers, freezes security gates and `sameAccuracyBand`, injects the real benchmark, and promotes only `Better AND Safe AND Authorized AND Reversible`. |
| [ADR-278](./ADR-278-darwin-tier2-sandbox-gate-closure.md) | Closing the Tier-2 `agent` sandbox's missing ADR-071 safety gate | Accepted (implemented) | `tier2-sandbox.ts` never called `inspectVariant` before executing a variant's real surface code — disclosed but left open in ADR-273. Now gates first, matching `sandbox.ts`/`llm-agent-sandbox.ts`; non-vacuous proof (a real side-effect marker file) confirms the bypass was real and is now closed. |

## Conventions used across the series

- **"Kernel"** = `@metaharness/kernel`, the package extracted from ruflo that contains primitives a harness needs regardless of identity or content (MCP wiring, hooks runtime, memory bridge, routing). Defined in ADR-002.
- **"Harness"** = a generated, npm-publishable package that wraps the kernel with chosen content (agents, skills, plugins) and identity (name, brand, scope). Defined in ADR-001.
- **"Content"** = agents, skills, prompts, plugins, vertical-specific code. Lives in the harness, not the kernel. Defined in ADR-002.
- **"Host"** = the agentic CLI or platform that runs the harness (Claude Code, Codex, pi.dev, Hermes). Defined in ADR-004.
- **"Composer"** = the interactive picker in `create-agent-harness` that lets a user choose agents, skills, plugins, primitives. Defined in ADR-003.
- **Powered-by**: a generated harness that ships kernel as a peer dep and links back to ruflo branding. Default mode.
- **Independence**: a generated harness that vendors the kernel (or hard-forks it) and ships under its own brand. Alternative mode. Defined in ADR-015.

## How to amend this series

- Edits to a Proposed ADR happen in PRs that reference the ADR number.
- A ratified ADR (`Status: Accepted`) is amended by a follow-on ADR (`Status: Supersedes ADR-NNN`) and never edited in place.
- New ADRs append to the series — they do not renumber.
- Cross-references between ADRs use the ADR number, not the slug, so renames do not break links.

## What to do next

If you are reading this with intent to start implementation, the gating ADR is **ADR-002 (Kernel boundary)**. Until that is accepted, every subsequent phase is blocked. ADR-002 is the highest-priority review.
