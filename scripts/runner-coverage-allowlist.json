{
  "$comment": [
    "Deliberate exclusions from runner coverage. See scripts/check-runner-coverage.mjs.",
    "This is a RATCHET seeded with the state on the day the check landed, so it passes on main.",
    "Every entry needs a non-empty reason: an allowlist without reasons is a mute button, and",
    "the next reader cannot tell a decision from an oversight. Deleting an entry is the goal."
  ],
  "tests": {
    "__tests__/adr-index.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/agent-harness-generator-lib.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/audit-deps.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/bench-baseline.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/claude-marketplace-plugin.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/cli-flags-completions.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/codex-skills.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/dev-toolkit.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/doctor-fail-message.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/e2e-lifecycle.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/e2e-scaffold-validate.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/examples-education.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/examples-federation.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/examples-quickstart.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/examples-vertical-tour.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-audit-bundle.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-compare.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-diag.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-export-config.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-genome.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-oia-manifest.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-sbom-audit.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-score.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-threat-model.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/harness-wizard.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/healthcheck.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/integration/host-functional.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/manifest-kernel-version.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/marketplace-entry.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/mcp-cmd.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/mcp-dispatch-integration.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/metaharness-subcommands.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/pack-contents.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/path-handling.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/publish-cmd.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/publish-workspace.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/release-notes.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/release.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/sbom.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/upgrade-cmd.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/validate-gcp-secrets.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/validate-oia-gate.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/version-bump.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/witness-tamper.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "__tests__/workflows.test.ts": "repo root: `npm test` is `npm run -ws --if-present test`, and `-ws` excludes the root package, so nothing runs the root suite today. Tracked in #194 — remove this entry once a runner covers it.",
    "kimi-k3-harness/__tests__/kernel.test.ts": "`kimi-k3-harness` sits at the repo root, outside the `packages/*` workspaces glob, so its own `\"test\": \"vitest run\"` script is never invoked. Tracked in #194.",
    "kimi-k3-harness/__tests__/smoke.test.ts": "`kimi-k3-harness` sits at the repo root, outside the `packages/*` workspaces glob, so its own `\"test\": \"vitest run\"` script is never invoked. Tracked in #194.",
    "services/apicompletions/tests/auth.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194.",
    "services/apicompletions/tests/budget.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194.",
    "services/apicompletions/tests/endpoints.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194.",
    "services/apicompletions/tests/firewall.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194.",
    "services/apicompletions/tests/health.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194.",
    "services/apicompletions/tests/messages.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194.",
    "services/apicompletions/tests/metering.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194.",
    "services/apicompletions/tests/routing.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194.",
    "services/apicompletions/tests/streaming.test.ts": "`services/*` is outside the root `workspaces` globs (`packages/*`) and no workflow targets it. Tracked in #194."
  },
  "crates": {
    "kimi-k3-harness/crates/k3-kernel-bench": "nested crate with its own [workspace], so root `cargo test --workspace` correctly skips it; nothing else runs it either. Tracked in #194. (1 #[test] fns)",
    "kimi-k3-harness/crates/k3rs": "nested crate with its own [workspace], so root `cargo test --workspace` correctly skips it; nothing else runs it either. Tracked in #194. (9 #[test] fns)",
    "packages/horizon/crate": "nested crate with its own [workspace], so root `cargo test --workspace` correctly skips it; nothing else runs it either. Tracked in #194. (14 #[test] fns)",
    "packages/oo-agents/crate": "nested crate with its own [workspace], so root `cargo test --workspace` correctly skips it; nothing else runs it either. Tracked in #194. (20 #[test] fns)"
  }
}
